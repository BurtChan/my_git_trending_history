# Quarkdown 项目分析

## 项目名称

**Quarkdown** — 拥有超能力的 Markdown：从灵感到论文、演示文稿、网站、书籍和知识库

- **GitHub**: [iamgio/quarkdown](https://github.com/iamgio/quarkdown)
- **许可证**: GNU GPLv3（CLI 和 Language Server 模块为 AGPLv3）

---

## 项目概述

Quarkdown 是一个现代化的基于 Markdown 的排版系统，旨在提供极强的通用性和灵活性。它允许用户通过单一项目，无缝编译出印刷级书籍、学术论文、知识库或交互式演示文稿等多种输出格式。作为 CommonMark 和 GitHub Flavored Markdown (GFM) 的扩展，Quarkdown 为 Markdown 引入了函数、变量和脚本编程能力，使其从一种静态标记语言升级为图灵完备的动态文档语言。

该项目由开发者 iamgio 于 2024 年 1 月创建，目前已在 GitHub 上获得了超过 11,000 颗星，并登上了 GitHub Trending 每日榜单。Quarkdown 的核心理念是"一次编写，多端输出"——同一个 `.qd` 文档可以根据不同的 doctype 设置，编译为 HTML 网页、PDF 文档、分页排版的论文/书籍、演示幻灯片或文档站。

Quarkdown 内置了一套丰富的标准库，涵盖布局管理、I/O 操作、数学计算和控制结构等模块，同时支持用户自定义函数和变量。配合 VS Code 扩展提供的实时预览功能，开发者可以在编写过程中即时查看渲染效果，极大地提升了文档创作效率。

---

## 核心功能

| 功能 | 描述 |
|------|------|
| **多格式编译** | 支持编译为 HTML、纯文本、PDF、分页文档（论文/书籍）、幻灯片演示和文档站 |
| **函数扩展** | 在 Markdown 中引入函数调用语法，实现动态内容生成 |
| **变量系统** | 支持在文档中定义和使用变量，实现内容复用和模板化 |
| **脚本编程** | 提供图灵完备的脚本支持，可在 Markdown 中编写复杂逻辑 |
| **DOCTYPE 系统** | 通过 `.doctype {plain/paged/slides/docs}` 切换输出格式 |
| **实时预览** | VS Code 扩展支持实时预览，配合 watch 模式自动刷新 |
| **标准库** | 内置布局、I/O、数学运算、控制结构等标准库函数 |
| **自定义函数** | 用户可创建自定义函数，扩展 Markdown 的表达能力 |
| **项目向导** | 提供 `quarkdown create` 命令快速创建新项目 |
| **GitHub Actions** | 支持 CI/CD 集成，自动化文档编译流程 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| **主要语言** | Kotlin |
| **许可证** | GNU GPLv3 / AGPLv3 |
| **编辑器支持** | VS Code 扩展 |
| **包管理** | Homebrew (macOS)、Scoop (Windows) |
| **自动化** | GitHub Actions |
| **基础规范** | CommonMark、GitHub Flavored Markdown (GFM) |
| **官网** | quarkdown.com |

---

## 项目亮点

### 🔄 一次编写，多端输出
Quarkdown 最突出的亮点在于其统一的多格式编译能力。用户只需维护一个 `.qd` 源文件，即可通过简单的 doctype 声明切换输出目标——无论是用于屏幕阅读的 HTML、用于打印的 PDF、用于演示的幻灯片，还是用于文档管理的知识库站点，都能从同一份源码生成。

### 🧠 图灵完备的 Markdown 扩展
不同于其他 Markdown 扩展仅添加语法糖，Quarkdown 引入了完整的脚本编程能力，使 Markdown 成为一种图灵完备的语言。这意味着用户可以在文档中实现条件逻辑、循环、函数定义等编程概念，生成传统 Markdown 无法实现的动态内容。

### ⚡ 实时预览与快速编译
配合 VS Code 扩展，Quarkdown 提供了流畅的实时预览体验。开发者可以使用 `quarkdown c file.qd -p -w` 命令开启 watch 模式下的实时预览，修改文档后即时看到渲染结果，大幅提升写作和排版效率。

### 📦 丰富的标准库生态
Quarkdown 内置了涵盖布局管理、文件 I/O、数学运算、字符串处理、控制结构等功能的标准库，无需额外安装依赖即可使用。同时，用户还可以通过自定义函数进一步扩展功能边界。

---

## 应用场景

### 📝 学术论文写作
借助 `.doctype {paged}` 模式和 PDF 输出，研究者可以使用 Quarkdown 编写格式规范的学术论文。变量和函数能力使得引用管理、公式编号、图表自动化等任务变得轻松高效。

### 🎤 演示文稿制作
通过 `.doctype {slides}` 模式，用户可以从同一份 Markdown 源文件生成交互式演示幻灯片，避免了在 PowerPoint 和 Markdown 之间反复切换的麻烦，适合技术分享和会议演讲。

### 📚 技术文档与知识库
Quarkdown 的 `.doctype {docs}` 模式适合构建项目文档站和知识库。配合 GitHub Actions 自动化编译和部署，团队可以轻松维护高质量的在线文档系统。

### 🌐 静态网站生成
对于个人博客或项目官网，Quarkdown 可以作为静态网站生成器使用，利用其 HTML 输出模式和丰富的排版能力，快速构建美观的网页内容。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| **总 Stars** | ~11,476 |
| **总 Forks** | 304 |
| **今日新增 Stars** | ~162 (from trending) |
| **许可证** | GNU GPLv3 / AGPLv3 |
| **主要语言** | Kotlin |

---

## 总结

Quarkdown 是一个雄心勃勃的现代化排版系统项目，它成功地将 Markdown 从静态标记语言提升为具备编程能力的动态文档语言。凭借其"一次编写、多端输出"的核心设计理念、图灵完备的脚本扩展、丰富的标准库以及优秀的编辑器集成体验，Quarkdown 为学术写作、技术文档、演示制作等领域提供了一个极具潜力的统一解决方案。作为一个使用 Kotlin 构建的开源项目，它在短短一年多时间里便获得了超过 11,000 颗星，充分体现了社区对其创新理念和实用价值的认可。
