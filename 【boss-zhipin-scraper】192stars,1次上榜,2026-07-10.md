# BOSS直聘爬虫 项目分析

## 项目名称

**boss-zhipin-scraper** — 基于 Chrome DevTools Protocol 的 BOSS直聘职位数据抓取工具

- **GitHub**: [eatmoreduck/boss-zhipin-scraper](https://github.com/eatmoreduck/boss-zhipin-scraper)
- **许可证**: MIT

---

## 项目概述

BOSS直聘爬虫是一款专为国内最大招聘平台之一 BOSS直聘 打造的数据采集工具，旨在帮助求职者和数据分析从业者高效获取职位信息。与传统爬虫工具依赖 Selenium 或 Playwright 等重量级浏览器自动化框架不同，该项目另辟蹊径，采用 Chrome DevTools Protocol（CDP）直接连接用户本地已登录的 Chrome 浏览器，从而复用真实的登录态来调用搜索 API，巧妙绕过了平台常见的反爬机制。

该项目解决的核心痛点是 BOSS直聘的字体反爬策略——薪资数字被替换为自定义字体，导致普通爬虫只能抓到乱码。boss-zhipin-scraper 通过 CDP 直连的方式获取原始 API 响应数据，成功绕过字体混淆，直接拿到明文薪资信息。此外，抓取过程使用持久隔离的 Chrome CDP Profile，避免污染用户的日常浏览器环境。

在数据输出方面，项目支持 JSON 和 CSV 双格式导出，并可对抓取结果进行深度分析，生成包括薪资区间分布、技能词频统计、JD 高频关键词在内的聚合摘要。更令人眼前一亮的是，它还能基于抓取数据自动生成求职优化提示词，帮助用户补齐简历关键词、改写项目经历、准备面试重点，形成从数据采集到求职优化的完整闭环。

项目的增量写入机制确保即使中途异常退出也不会丢失已采集的数据，体现了对实际使用场景的细致考量。作为开源项目，它为招聘市场数据分析和求职效率提升提供了极具实用价值的工具。

---

## 核心功能

- **CDP 直连抓取**：通过 Chrome DevTools Protocol 连接本地 Chrome，复用真实登录态调用搜索 API
- **绕过字体反爬**：直接获取明文薪资数据，无需处理字体混淆
- **多维筛选条件**：支持按公司规模、融资阶段、薪资范围、工作经验、学历要求、行业领域进行筛选
- **详情页 JD 抓取**：可深入抓取职位详情页的完整岗位描述
- **双格式输出**：支持 JSON 和 CSV 两种数据导出格式
- **聚合分析摘要**：自动生成薪资区间分布、技能词频统计、JD 高频词分析
- **求职优化提示词**：基于抓取数据生成简历关键词补齐、项目改写、面试准备建议
- **增量写入**：异常退出不丢失已采集数据，支持断点续爬
- **持久隔离 Profile**：独立 Chrome CDP Profile，不影响用户日常浏览器

---

## 技术栈

| 技术 | 用途 |
|------|------|
| Python | 主开发语言，编写爬虫逻辑和数据分析模块 |
| Chrome DevTools Protocol (CDP) | 连接本地 Chrome 浏览器，复用登录态调用 API |
| JSON / CSV | 数据输出格式 |
| Chrome 浏览器 | 作为 CDP 连接目标，提供真实登录环境 |

---

## 项目亮点

1. **创新的 CDP 直连方案**：摒弃了 Selenium 和 Playwright 等传统浏览器自动化工具的沉重负担，直接通过 CDP 协议与本地 Chrome 通信，不仅性能更高，而且完全复用用户已有的登录状态，避免了模拟登录的种种麻烦和风险。

2. **精准突破字体反爬**：BOSS直聘采用自定义字体替换薪资数字的反爬手段是业界公认的难题。该项目通过 CDP 获取原始 API 响应，从根本上绕过了字体混淆层，直接拿到明文薪资数据，技术方案简洁而高效。

3. **从数据采集到求职优化的全链路闭环**：不仅抓数据，还提供聚合分析（薪资分布、技能词频、JD 高频词）和 AI 辅助求职优化（简历关键词、项目改写、面试准备），将爬虫工具的价值延伸到求职决策的下游环节。

4. **健壮的增量写入机制**：在实际爬取场景中，网络波动或异常中断是常态。项目支持增量写入，确保已抓取的数据不会因意外退出而丢失，大幅提升了大批量数据采集的可靠性。

5. **轻量且隐私友好**：通过隔离的 CDP Profile 运行，不触碰用户日常浏览器数据，同时不需要服务器端部署，在本地即可完成全部操作，保障了求职数据的隐私安全。

---

## 应用场景

1. **求职市场调研**：求职者在投递简历前，可以通过该工具系统性地采集目标岗位的薪资分布、技能要求、公司背景等数据，从而制定更有针对性的求职策略和薪资谈判方案。

2. **招聘数据分析**：HR 从业者和行业分析师可以利用批量采集的职位数据，进行行业薪资趋势分析、人才需求热点挖掘、竞品公司招聘动态跟踪等深度研究，为招聘决策和行业报告提供数据支撑。

3. **简历优化与面试准备**：基于抓取到的 JD 高频关键词和技能词频，求职者可以精准补齐简历中的关键术语，并根据目标岗位的要求重新措辞项目经历，同时针对高频技能点进行有针对性的面试准备。

4. **批量职位监控**：对于需要持续关注特定类型岗位变化的用户，可以定期运行爬虫进行增量采集，追踪薪资波动、新增岗位、企业招聘动态等变化趋势，及时把握市场机会。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| **总 Stars** | 192 |
| **总 Forks** | 27 |
| **许可证** | MIT |
| **主要语言** | Python |

---

## 总结

boss-zhipin-scraper 是一款定位精准、技术方案优雅的招聘数据采集工具。它通过 CDP 直连的创新路径巧妙绕过了 BOSS直聘的登录验证和字体反爬两大障碍，在保证数据完整性的同时大幅降低了使用门槛。项目不仅关注数据采集本身，还延伸到了聚合分析和求职优化，形成了完整的价值链条。

从工程角度来看，增量写入、隔离 Profile 等设计细节体现了开发者对实际使用场景的深入理解。作为开源项目，MIT 许可证和 Python 技术栈使其易于被社区采纳和二次开发。对于国内求职者和招聘行业从业者而言，这是一个极具实用价值的工具。

---

*数据来源：GitHub 仓库 (eatmoreduck/boss-zhipin-scraper)，分析日期 2026年7月10日*
