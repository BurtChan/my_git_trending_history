# Zod 项目分析

## 项目名称

**Zod** — TypeScript-first 的 Schema 校验库（静态类型推断）

- **GitHub**: [colinhacks/zod](https://github.com/colinhacks/zod)
- **许可证**: MIT

---

## 项目概述

Zod 是 TypeScript 生态中最主流的运行时校验库，由 Colin McDonnell（colinhacks）开发：定义一个 schema，用它 parse 任意不受信任的数据，你会得到一个强类型、已校验的深拷贝结果。它的杀手锏是「schema 即类型源」——`z.infer<typeof User>` 从 schema 定义直接推断出静态类型，一份定义同时约束编译期类型与运行时校验，消灭了「interface 与校验逻辑两处维护」的经典漂移问题。

零依赖、2KB 核心（gzip）、不可变 API、Node 与全现代浏览器通吃、内置 JSON Schema 转换，这些特质让 Zod 成为 tRPC、React Hook Form、LangChain 等重量级框架的底层依赖，43.5K Star 背后是事实上的生态标准地位。近期最重磅的进展是 **AOT 编译**（canary 通道）：`z.compile(schema)` 为热路径生成提前编译的快速通道，55 个 schema 的基准中位数提速 2.4 倍，大对象数组场景可达 9 倍——这是 Zod 面向高性能场景的一次关键进化，也再次带热了社区讨论。

---

## 核心功能

| 功能 | 说明 |
|------|------|
| Schema 定义 | `z.object/z.string/z.number` 等组合式 schema 构建 |
| parse/safeParse | 校验并返回强类型深拷贝；safeParse 返回判别联合结果对象 |
| 类型推断 | `z.infer<>` 从 schema 提取静态类型，`z.input/z.output` 区分变换前后 |
| 精细报错 | ZodError 携带逐字段 issue 列表（路径/期望类型/消息） |
| AOT 编译 | `z.compile(schema)` 提前编译快速路径，中位数 2.4x 提速（canary） |
| JSON Schema | 内置双向 JSON Schema 转换 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| **核心语言** | TypeScript |
| **体积** | 2KB 核心（gzip），零外部依赖 |
| **构建** | pnpm monorepo（packages/ 多包）、Vitest 测试 |
| **工程协作** | .claude/skills、.codex、CLAUDE.md——原生 AI 协作开发配置 |
| **许可** | MIT |

---

## 项目亮点

### 1. Schema 即唯一事实源
类型与校验同源，API 响应、表单输入、环境变量、配置文件统一用一套 schema 守门。

### 2. AOT 编译补齐性能短板
`new Function` 生成编译路径、无效输入自动回退常规解析器保证报错一致性，CSP 环境可 jitless 降级——性能与安全兼顾。

### 3. 生态寄生标准地位
tRPC/RHF/LangChain 等头部框架的默认校验层，学会 Zod 约等于打通 TS 全栈类型安全链路。

### 4. 极致轻量
2KB 核心做到上述全部能力，任何端（浏览器/边缘/Node）零负担引入。

---

## 应用场景

### 1. API 边界校验
在 HTTP handler 入口校验请求体，配合 tRPC 实现端到端类型安全。

### 2. 表单校验
React Hook Form resolvers 集成，schema 直转表单校验规则。

### 3. LLM 结构化输出
LangChain/AI SDK 用 Zod schema 约束模型输出 JSON 的结构。

### 4. 环境变量与配置守门
启动时校验 .env 与配置文件，缺失/类型错误立即失败。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| 总 Stars | 43,576 |
| 总 Forks | 2,154 |
| 今日新增 Stars | +17 |
| 主要语言 | TypeScript |
| 许可证 | MIT |
| 创建时间 | 2020-03-07 |

---

## 总结

TypeScript 世界「运行时校验」的默认答案：一份 schema 同时喂饱编译器和运行时，AOT 编译又把性能焦虑也解决了——43K Star 的生态标准仍在进化。

---

*数据来源：GitHub 仓库 (colinhacks/zod)，2026 年 8 月访问*
