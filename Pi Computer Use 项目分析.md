# Pi Computer Use 项目分析

## 项目名称
**Pi Computer Use** — 让 Pi AI 代理通过视觉理解和 UI 操作控制桌面应用程序
- **GitHub**: [injaneity/pi-computer-use](https://github.com/injaneity/pi-computer-use)
- **许可证**: MIT

---

## 项目概述

Pi Computer Use 是 Pi 编码代理的扩展插件，使 AI 代理能够直接与 macOS 和 Windows 上的桌面应用程序进行交互。代理可以观察应用窗口的界面元素（按钮、文本、控件），理解其含义，并执行点击、输入文字、滚动、等待变化等操作。这填补了 AI 代理与无 API 桌面应用之间的交互空白——当某个应用没有公开的 API 或 MCP 服务端接口时，代理仍能通过"看"和"操作"屏幕来完成复杂任务。

该扩展由 Pi 生态的开发者 injaneity 构建，基于平台原生的无障碍（Accessibility）API 实现跨平台支持。其核心设计理念是"语义化计算机使用"（Semantic Computer Use）：代理操作的不是像素坐标，而是通过无障碍树中的语义化 UI 元素引用来定位和操控界面。这使得操作比传统的屏幕截图+坐标点击方式更加可靠和鲁棒。

项目采用不可变、状态作用域观测的架构设计，桌面表面和 CDP 页面形成多根树结构，渐进式大纲查询保持缓存，活工作按物理资源有序排列，独立根可以并行运行。`act_ui` 工具接受一个或多个意图步骤，跨依赖输入保持焦点，验证交付，安全恢复，存储一个完整的后继状态，并在身份置信度允许时返回紧凑差异。

---

## 核心功能

| 工具 | 功能 |
|------|------|
| `find_roots` | 发现当前打开的应用和窗口 |
| `observe_ui` | 观察窗口中可见的界面内容 |
| `search_ui` | 在可见界面中搜索文本、按钮、控件 |
| `expand_ui` | 检查界面特定部分的更多细节 |
| `inspect_ui` | 深度检查界面元素属性和状态 |
| `act_ui` | 点击、输入、滚动、按下 UI 控件 |
| `read_text` | 从 UI 中读取文本内容 |
| `wait_for` | 等待界面发生变化 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 核心语言 | TypeScript (36.3%) |
| 系统层 | Rust (26.8%) |
| macOS 原生层 | Swift (22.8%) |
| 辅助脚本 | JavaScript (13.8%), Shell (0.3%) |
| 平台接口 | macOS Accessibility API / Windows UI Automation |
| 安装方式 | `pi install npm:@injaneity/pi-computer-use` |

---

## 项目亮点

### 语义化 UI 操作替代坐标点击

与传统的基于屏幕截图和像素坐标的计算机使用方案（如 Anthropic 的 Computer Use）不同，Pi Computer Use 通过平台原生的无障碍 API 访问语义化的 UI 元素树。代理操作的是"点击标记为'确认'的按钮"而非"点击坐标 (450, 320)"，这种方式在窗口大小变化、分辨率不同或 UI 动态更新时仍然可靠。

### 跨平台架构设计

项目同时支持 macOS 和 Windows 两个主流桌面平台，分别使用各自平台的无障碍 API（macOS 的 Accessibility / TCC 权限系统，Windows 的 UI Automation API）。代码库中 macOS 原生层使用 Swift（22.8%），性能关键路径使用 Rust（26.8%），业务逻辑层使用 TypeScript（36.3%），形成清晰的分层架构。

### Pi 生态的无缝集成

作为 Pi 编码代理的扩展插件，Pi Computer Use 通过标准的扩展机制安装和运行。Pi 本身是一个极简的终端 AI 编程代理（仅 4 个核心工具），所有高级功能都通过扩展实现。这种"核心极简+扩展丰富"的架构使得 Computer Use 功能可以按需安装，不增加基础代理的复杂度。

### 不可变状态架构

项目采用不可变状态作用域观测的设计模式——每次 UI 操作都产生一个完整的后继状态快照，而非在可变状态上累积修改。这种设计使得操作回滚、并发执行和错误恢复更加自然和安全，特别适合 AI 代理这种可能需要反复试错的交互场景。

---

## 应用场景

### 无 API 桌面应用的自动化测试

许多桌面应用（特别是企业内部工具、旧版软件）没有公开的 API 或自动化测试接口。Pi Computer Use 可以让 AI 代理像人类用户一样操作这些应用的界面，实现端到端的自动化测试，无需额外的测试框架或 SDK 集成。

### 跨应用工作流编排

AI 代理经常需要在多个应用之间切换来完成复杂任务——例如从浏览器复制数据到电子表格、在邮件客户端中查找信息并整理到文档中。Pi Computer Use 使代理能够跨越应用边界，通过统一的界面操作接口编排跨应用工作流。

### 辅助功能和无障碍支持

对于需要辅助技术的用户，Pi Computer Use 提供了一种将 AI 代理能力与桌面应用连接起来的途径。代理可以辅助用户导航复杂界面、填写表单、完成多步骤操作，降低软件使用门槛。

### 开发者桌面自动化

开发者在日常工作中频繁与 IDE、终端、浏览器、文件管理器等桌面应用交互。Pi Computer Use 可以将这些交互也纳入 AI 代理的能力范围，使代理不仅限于代码编辑和终端命令，还能操作图形化的开发工具。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| 总 Star | 1,312 |
| 总 Fork | 82 |
| 今日新增 | 103 |
| 主要语言 | TypeScript |
| 创建时间 | 2026-04-20 |

---

## 总结

Pi Computer Use 是 Pi 编码代理生态中填补"AI 代理 ↔ 桌面应用"交互空白的关键扩展。通过语义化 UI 操作和跨平台无障碍 API，它让 AI 代理能够像人类一样操控桌面软件，打开了桌面自动化的新可能性。其不可变状态架构和 Pi 的极简扩展机制相结合，代表了"Computer Use"领域一条与像素级操作不同的技术路线。

---

*数据来源：GitHub 仓库 (injaneity/pi-computer-use)，2026 年 7 月访问*
