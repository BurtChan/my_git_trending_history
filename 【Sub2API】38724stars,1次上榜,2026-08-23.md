# Sub2API 项目分析

## 项目名称
**Sub2API** — 一站式开源 AI 订阅中转服务，让 Claude、OpenAI、Gemini、Grok 订阅统一接入
- **GitHub**: [Wei-Shaw/sub2api](https://github.com/Wei-Shaw/sub2api)
- **许可证**: LGPL-3.0
- **语言**: Go（后端）+ Vue 3（前端）

---

## 项目概述

Sub2API 是一个自托管的开源中转网关，核心定位是「订阅转 API」：把用户手上已有的 Claude、OpenAI（Codex）、Gemini、Grok 等 AI 订阅账号，统一转换成标准 OpenAI 兼容 API 端点对外提供服务。它由 Wesley Liddick（Wei-Shaw）开发，2025 年 12 月创建，短短八个月 star 数逼近 3.9 万，是 AI 基础设施赛道中增长最快的自托管项目之一。

项目切中的痛点非常具体：官方订阅（如 ChatGPT Plus/Pro、Claude Pro、Grok 订阅）通常只能在官方客户端中使用，而大量开发者希望在自己的工具链（Claude Code、Codex CLI、各类 ChatBox）里以原生方式消费订阅额度；同时「拼车共享」场景下，多个用户分摊一个订阅的成本，需要账号池调度与配额隔离。Sub2API 用一个 Go 网关同时解决了这两件事——上游多账号池化管理，下游暴露统一 API，原生工具无缝接入。

架构上采用前后端分离：Go 后端（cmd/server + internal 的 gateway/service/handler 分层）承担 API 网关与调度核心，Vue 3 前端提供管理后台，配套 PostgreSQL + Redis，支持 Docker Compose 一键部署与 goreleaser 二进制发布，5,900+ commits 显示开发节奏相当密集。

---

## 核心功能

| 功能 | 描述 |
|------|------|
| 订阅转 API | 将 Claude/OpenAI/Gemini/Grok 订阅账号转换为 OpenAI 兼容 API 端点（/v1/responses 等） |
| 多账号池管理 | 支持 OAuth 订阅账号与 API Key 账号混合接入、自动调度与故障转移 |
| 拼车共享 | 多用户分摊订阅成本，按用户/渠道隔离配额，适合小团队合租场景 |
| Grok OAuth 接入 | 完整实现 xAI OAuth PKCE 流程，订阅账号可直接转发 Responses 流量 |
| 渠道与配额控制 | 管理后台可创建渠道、绑定账号、设置 URL 白名单与安全策略 |
| 媒体生成资格检查 | Grok 图像/视频生成前主动探测计费资格，非付费订阅自动隔离 |
| 管理员 API | OAuth 授权 URL 生成、授权码交换等全流程可通过 admin API 自动化 |
| Claude Skills 集成 | 仓库内置 skills/sub2api-admin 技能，可用 AI 助手直接运维管理 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 后端 | Go（分层架构：gateway / service / handler） |
| 前端 | Vue 3 + Pinia |
| 数据库 | PostgreSQL + Redis |
| 部署 | Docker Compose / Apple Container / goreleaser 二进制 / install.sh 一键脚本 |
| 协议 | OpenAI 兼容 API、xAI OAuth PKCE |

---

## 项目亮点

### 订阅额度最大化利用
把「只能在官方 App 里用」的订阅变成「任何支持 OpenAI API 的工具都能用」，Claude Code、Codex CLI 等原生工具无需改造即可指向自建端点，订阅性价比直接拉满。

### 拼车成本分摊
针对国内 AI 圈流行的「合租订阅」场景做了产品化支持：账号池 + 用户配额隔离 + 管理后台，把灰色地带的拼车行为变成可控的自托管服务。

### 工程完成度高
5,900+ commits、完整的 OAuth PKCE 实现、媒体资格探测、URL 白名单安全模式、goreleaser 发布链路，开源规范（CLA、DEV_GUIDE、openspec）一应俱全，不是玩具项目。

---

## 应用场景

### 小团队共享 AI 订阅
团队合租一个 ChatGPT Pro / Claude Max 订阅，通过 Sub2API 分发给每个成员独立 key，统一计费与限额。

### 统一多模型网关
同时持有 Claude、Gemini、Grok 多家订阅的开发者，用一个本地网关统一收口，应用层只对接一个端点。

### 原生 CLI 工具接入订阅
让 Claude Code / Codex CLI 等原本只认官方账号的工具，直接消费已有订阅额度。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| 总 Stars | 38,724 |
| 总 Forks | 8,030 |
| 今日新增 | +264 |
| 创建时间 | 2025-12-18 |

---

## 总结

Sub2API 把「AI 订阅拼车 + 订阅转 API」这件需求明确但工程繁琐的事做成了开箱即用的自托管网关，Go + Vue 技术栈成熟、部署链路完善、8 个月近 3.9 万 star 的增速印证了需求的真实性，是 AI 基础设施自托管浪潮中的标志性项目。

---

*数据来源：GitHub 仓库 (Wei-Shaw/sub2api)，2026 年 8 月访问*
