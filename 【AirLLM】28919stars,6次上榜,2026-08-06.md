# AirLLM 项目分析

## 项目名称

**AirLLM** — 在单张 4GB 显卡上运行 70B 大语言模型的推理优化框架

- **GitHub**: [lyogavin/airllm](https://github.com/lyogavin/airllm)
- **许可证**: Apache-2.0

---

## 项目概述

AirLLM 是由 lyogavin 创建的**大语言模型推理优化框架**，其最令人瞩目的能力是：在仅 4GB 显存的消费级 GPU 上运行 70B 参数的大语言模型——无需量化、蒸馏或剪枝。项目于 2023 年中发布，迅速在 AI 社区引起广泛关注。

AirLLM 的核心技术是**分块逐层推理**（Layered Inference）。传统的 LLM 推理需要将整个模型的参数一次性加载到 GPU 显存中（70B 模型约需 130GB+），而 AirLLM 巧妙地利用了 Transformer 架构的层级特性：每一层只依赖前一层的输出作为输入，因此可以逐层加载、计算、卸载。在推理时，GPU 每次只需加载一个 Transformer 层的参数（约 1.6GB，即全模型参数的 1/80），从而将显存需求从 130GB+ 降低到 4GB 以下。

项目后续还支持了 **405B Llama3.1** 在 8GB 显存上运行，进一步拓展了消费级硬件运行超大模型的边界。此外，AirLLM 还引入了基于分块量化的模型压缩技术，在几乎可忽略的精度损失下实现 3 倍推理加速。

---

## 核心功能

### 1. 超低显存推理
核心功能——在 4GB 显存的消费级 GPU 上运行 70B 级别的大语言模型，无需硬件量化、知识蒸馏或模型剪枝。通过逐层加载和卸载策略，将显存占用降至极低。

### 2. 多模型支持
支持广泛的 LLM 架构，包括 Llama2/Llama3/Llama3.1、ChatGLM、Qwen/Qwen2.5、Baichuan、Mistral、Mixtral、InternLM 等主流开源模型。内置 AutoModel 自动检测机制，无需手动指定模型类型。

### 3. 模型压缩与加速
提供基于分块量化的模型压缩功能，可实现最高 3 倍的推理速度提升，且精度损失极小（几乎可忽略）。支持 8bit 和 4bit 量化模式。

### 4. 跨平台支持
支持 Linux、macOS 和 Windows 平台，2023 年 12 月起支持在 MacOS 上运行 70B 大模型。支持 CPU 推理模式（v2.10.1+）。

### 5. 预取加速
内置预取机制（prefetching），通过重叠模型加载和计算来减少等待时间，进一步提升端到端推理吞吐量。

### 6. 简洁的 API 设计
提供与标准 HuggingFace Transformers 几乎一致的推理接口，迁移成本低：

```python
from airllm import AirLLMLlama2
model = AirLLMLlama2("meta-llama/Llama-2-70b-hf")
output = model.generate(["What is quantum computing?"])
```

---

## 技术栈

| 组件 | 技术 |
|------|------|
| **核心语言** | Python |
| **底层框架** | PyTorch, HuggingFace Transformers |
| **核心技术** | 分块逐层推理（Layered Inference）、分块量化（Block-wise Quantization） |
| **模型格式** | HuggingFace safetensors、GGUF |
| **量化模式** | 无量化（原生）、8bit、4bit |
| **平台** | Linux, macOS, Windows |
| **GPU 支持** | NVIDIA CUDA、Apple Metal (MPS) |
| **许可证** | Apache-2.0 |

---

## 项目亮点

### 突破硬件极限
AirLLM 的分块逐层推理技术从根本上改变了大模型部署的硬件门槛。传统方案需要 A100（80GB）或多卡集群才能运行 70B 模型，而 AirLLM 让普通开发者用一张 RTX 3050（4GB）就能体验 70B 模型的能力。这种"以时间换空间"的工程思路极为巧妙——推理速度虽然不及全量加载方案，但让原本不可能的硬件配置成为可能。

### 405B 模型的 8GB 显存支持
在 4GB 推理的基础上，AirLLM 进一步支持了 405B 参数的 Llama3.1 在 8GB 显存上运行。405B 是目前最大的开源模型之一，传统方案需要 4-8 张 A100 才能运行，而 AirLLM 将其硬件需求降低到了笔记本级别。

### 极简的迁移路径
项目保持了与 HuggingFace Transformers 几乎一致的 API 接口，开发者只需更换 import 和模型初始化代码即可从标准推理框架迁移到 AirLLM，迁移成本极低。同时支持 PyPI 安装（`pip install airllm`），开箱即用。

### 学术与工程结合
项目基于作者在 Kaggle 竞赛中的技术实践，将学术级的模型优化技术转化为工程可用的工具。分块量化的精度评估也有配套论文支撑，技术方案有充分的理论和实验依据。

---

## 应用场景

### 个人开发者实验
没有高性能 GPU 的个人开发者可以使用 AirLLM 在普通笔记本或入门级显卡上体验 70B 级别大模型的推理效果，进行原型验证和方案探索。

### 资源受限环境部署
在云服务器配额受限、GPU 资源紧张或仅能使用 CPU 的环境中，AirLLM 提供了一种可行的 LLM 部署方案，虽然推理速度较慢但功能完整。

### 模型效果评估
研究人员需要在本地快速评估不同 70B 模型的输出质量（而非性能指标）时，AirLLM 提供了低成本的评估环境，无需租用昂贵的 GPU 实例。

### 边缘计算场景
在带宽受限、计算资源有限但需要本地 LLM 能力的场景（如远程办公、舰船、野外作业站），AirLLM 可以在低配硬件上提供基础的大模型推理服务。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| **总 Stars** | 28,919 |
| **总 Forks** | 3,110 |
| **今日新增 Stars** | 833 |
| **许可证** | Apache-2.0 |
| **创建时间** | 2023 年 6 月 |
| **主要语言** | Jupyter Notebook / Python |
| **Open Issues** | 158 |

---



---

## 📋 更新记录

### 更新 6 — 2026年8月6日（再次登上 Trending）
**更新原因**：项目再次登上 GitHub Trending 榜单，Star 数从 28,104 增长至 28,919（+815），日增 833 颗 Star

**最新动态**：AirLLM 连续第五日登上 Trending，Star 数逼近 29K。项目主打「单 4GB GPU 推理 70B 大模型」的极低门槛推理方案持续吸引个人开发者，v3.1.0 支持 Kimi K3（2.8T 参数）单卡 3.72GB 推理的方案是近期最大亮点。在本地化部署需求旺盛的背景下，AirLLM 将「人人都能跑大模型」从口号变为现实，尤其适合消费级显卡与边缘设备场景，是低资源推理赛道的代表性项目。

**最新 Star 数据**：

| 指标 | 上次记录 | 最新数据 | 变化 |
|------|----------|----------|------|
| 总 Stars | 28,104 | 28,919 | +815 |
| 总 Forks | 3,068 | 3,110 | +42 |

**核心变化概要**：
- Star 数逼近 29,000，连续 5 日登上 Trending，累计增长显著
- v3.1.0 支持 Kimi K3（2.8T）单卡 3.72GB 推理，刷新超大模型低资源推理纪录
- FP8 量化与主流开源模型支持完善，Jupyter Notebook 生态教程丰富

---



### 更新 1 — 2026 年 8 月 2 日（再次登上 Trending）
**更新原因**：项目再次登上 GitHub Trending 榜单

**最新动态**：
AirLLM 项目在模型支持范围上取得了重大突破。除了原有的 Llama 系列和 DeepSeek V2/V3，项目新增了对 Kimi K3（2.8T 参数——最大的开源模型）的支持，实测仅需 3.72GB VRAM 即可在 RTX 6000 Ada 上运行。此外还新增了对 Llama 4、Qwen 3 系列和 Mixtral MoE 模型的完整支持。技术架构方面，AirLLM 的分块逐层推理技术在 MoE（Mixture of Experts）模型上展现了独特优势：对于稀疏 MoE 架构，每次只流式传输一个专家的数据，使 2.8T 参数的模型也能在消费级 GPU 上运行。项目还改进了模型压缩技术（Block-wise Quantization），支持 4-bit 和 8-bit 量化选项，在几乎可忽略精度损失的情况下实现 3 倍推理加速。

**最新 Star 数据**：

| 指标 | 上次记录 | 最新数据 | 变化 |
|------|----------|----------|------|
| 总 Stars | 18,835 | 25,235 | +6,400 |
| 总 Forks | 2,067 | 2,841 | +774 |

**核心变化概要**：
- Star 数从 18,835 增至 25,235（+6,400），增长 34%
- 新增 Kimi K3（2.8T）支持，仅 3.72GB VRAM 即可运行最大开源模型
- 新增 Llama 4、Qwen 3 系列、Mixtral MoE 模型完整支持
- 稀疏 MoE 专家流式传输技术，单专家按需加载突破硬件极限
- Block-wise 量化支持 4-bit/8-bit，3 倍推理加速且精度损失极小
### 更新 2 — 2026 年 8 月 3 日（再次登上 Trending）
**更新原因**：项目再次登上 GitHub Trending 榜单

**最新动态**：
AirLLM 发布了 v3.1.0 版本（2026 年 7 月 29 日），核心亮点是正式支持 Kimi K3（2.8T 参数——目前最大的开源模型）。K3 的架构具有三个不可选的特殊依赖：compressed-tensors、flash-attn 和 transformers 4.56.x。团队在真实 2.8T checkpoint 上发现并修复了合成测试无法覆盖的五个流式生成 Gap，确保端到端生成正确。此外更新了 README 文档，将 Kimi K3 的「单卡 3.72GB」作为 Hero 展示。项目目前支持从 8B 到 2.8T 参数的全系列模型，覆盖 Llama、Qwen、DeepSeek、Mistral、Kimi 等主流架构。

**最新 Star 数据**：

| 指标 | 上次记录 | 最新数据 | 变化 |
|------|----------|----------|------|
| 总 Stars | 25,235 | 25,662 | +427 |
| 总 Forks | 2,841 | 2,884 | +43 |

**核心变化概要**：
- Star 数从 25,235 增至 25,662（+427），发布 v3.1.0 后持续增长
- v3.1.0 正式支持 Kimi K3（2.8T），单卡 3.72GB 运行最大开源模型
- 修复真实 checkpoint 暴露的 5 个流式生成 Gap（合成测试无法覆盖）
- README 重新聚焦 Kimi K3 Hero 展示，强调极低 VRAM 优势
### 更新 3 — 2026 年 8 月 4 日（再次登上 Trending）
**更新原因**：项目再次登上 GitHub Trending 榜单

**最新动态**：
- 项目已支持**超大规模模型**：Kimi K3（2.8T 参数）可在 <4GB VRAM 上运行，DeepSeek-V3（671B）仅需约 12GB，Llama 3.1 405B 约 8GB
- 新增 **MacOS 支持**（通过 mlx 框架），拓展了苹果生态的开发者覆盖面
- **Block-wise 量化**技术带来 3 倍推理加速，精度损失几乎可忽略
- 全面覆盖主流架构：Llama、Qwen、DeepSeek、Mistral、Phi、Gemma、ChatGLM、Baichuan、InternLM、Yi、Kimi K3

**最新 Star 数据**：

| 指标 | 上次记录 | 最新数据 | 变化 |
|------|----------|----------|------|
| 总 Stars | 25,662 | 26,622 | +960 |
| 总 Forks | 2,884 | 2,944 | +60 |

**核心变化概要**：
- Star 数从 25,662 增至 26,622（+960），单日增幅创近期新高
- Kimi K3（2.8T）<4GB VRAM 运行，DeepSeek-V3（671B）~12GB，持续突破硬件极限
- MacOS mlx 支持打开苹果开发者生态，扩大用户群体
- Block-wise 量化 3 倍加速使低硬件配置下的实际可用性大幅提升
- 主流架构全覆盖（11 家），成为消费级硬件运行大模型的首选框架

## 总结

AirLLM 是**大模型推理优化领域的技术突破项目**。它通过创新的分块逐层推理技术，将 70B 参数大模型的 GPU 显存需求从 130GB+ 降低到 4GB，让消费级硬件也能运行超大模型。项目支持 Llama、Qwen、ChatGLM、Mistral 等主流模型架构，甚至能将 405B Llama3.1 运行在 8GB 显存上。v3.1.0 新增的 Kimi K3（2.8T）支持进一步拓展了硬件极限，2.8T 参数模型仅需 3.72GB VRAM。虽然推理速度不如全量加载方案，但其突破硬件极限的能力使其成为资源受限环境下运行大模型的独特选择。

### 更新 4 — 2026年8月4日（再次登上 Trending）

**最新 Star 数据**：

| 总 Stars | 26,622 | 27,573 | +951 |

- Star 数从 26,622 增至 27,573（+951），日增 1,085 颗 Star

**更新原因**：项目再次登上 GitHub Trending 榜单，Star 数从 26,622 增长至 27,573（+951），日增 1,085 颗 Star

Star 增长 951 颗，日增 1,085 颗（Trending）。AirLLM 70B inference with single 4GB GPU

> 更新依据：GitHub Trending 2026-08-04 数据，Star 数由 GitHub API 实时获取

---

*首次分析：见文件头部 | 最近更新：2026 年 8 月 6 日*

---

### 更新 5 — 2026年8月5日（再次登上 Trending）

**更新原因**：项目再次登上 GitHub Trending 榜单，Star 数从 27,573 增长至 28,104（+531），日增 1,138 颗 Star

**最新动态**：
- Star 数突破 28,000 大关，v3.1.0 支持的 Kimi K3（2.8T）单卡 3.72GB 推理方案持续引发关注
- Trendshift 数据显示 8月3日首次登顶 Trending #1，项目在 AI 推理优化领域影响力持续攀升
- 支持从 8B 到 2.8T 全系列模型，覆盖 Llama/Qwen/DeepSeek/Mistral/Kimi 等主流架构，FP8 支持 + 最新模型兼容

**最新 Star 数据**：

| 指标 | 上次记录 | 最新数据 | 变化 |
|------|----------|----------|------|
| 总 Stars | 27,573 | 28,104 | +531 |
| 总 Forks | 3,042 | 3,068 | +26 |

**核心变化概要**：
- Star 数增长 +531，日增 1,138 颗（Trending）。单 4GB GPU 推理 70B 大模型的推理优化框架，支持 FP8 和最新主流开源模型
