# Paseo 项目分析

## 项目名称

**Paseo** — 从手机、桌面和命令行运行 AI 编码代理

- **GitHub**: [getpaseo/paseo](https://github.com/getpaseo/paseo)
- **许可证**: AGPL-3.0

---

## 项目概述

Paseo 是一个跨平台的 AI 编码代理编排工具，其核心理念是"Ship from your phone or your desk"——让用户可以在任何设备上运行和管理 AI 编码代理（如 Claude Code、Codex、Copilot、OpenCode、Pi 等）。项目通过本地守护进程（daemon）+ 多端客户端（桌面应用、手机应用、Web 应用、CLI）的架构，实现了对多种编码代理的统一管理。

Paseo 解决了一个实际痛点：当前各种 AI 编码代理各有各的命令行界面，且大多只能在终端中使用。当你离开电脑、只能用手机时，就无法监控和管理正在运行的编码任务。Paseo 通过本地守护进程 + 安全中继的架构，让用户可以在手机上查看编码代理的实时输出、发送指令、甚至启动新的代理任务。

项目采用 TypeScript + Nix 的 Monorepo 架构，桌面和移动端应用功能完整，提供 Agent 切换、并行执行、中继连接等高级功能。Skills 系统允许代理通过 `/paseo-handoff`、`/paseo-loop`、`/paseo-advisor`、`/paseo-committee` 等命令实现多代理间的任务交接和协作。

---

## 核心功能

### 统一代理管理
支持 Claude Code、OpenAI Codex、GitHub Copilot、OpenCode、Pi 等多种主流 AI 编码代理的统一管理和切换。

### 多设备访问
提供桌面应用、手机应用、Web 应用和 CLI 四种客户端，通过 QR 码扫描即可从手机连接到本地守护进程。

### 并行执行
支持在本地机器上并行运行多个编码代理，提高开发效率。

### 代理编排 Skills
内置 Skills 系统支持代理间协作：handoff（任务交接）、loop（循环执行）、advisor（顾问模式）、committee（委员会决策）。

### 安全中继
支持 Tailscale 集成和自托管中继服务器，确保手机到本地代理的连接安全可靠。

### 代码搜索集成
内置代码搜索功能，可搜索仓库、Issues 和 Pull Requests。

---

## 技术栈

| 组件 | 技术 |
|------|------|
| **语言** | TypeScript + Nix |
| **架构** | Monorepo（pnpm workspaces） |
| **桌面应用** | Electron |
| **移动应用** | 跨平台移动框架 |
| **后端** | 本地守护进程（daemon） |
| **连接** | 安全中继（支持 Tailscale） |
| **CLI** | npm 包 `@getpaseo/cli` |

---

## 项目亮点

### 真正的移动编码代理体验
Paseo 是目前为数不多的支持从手机运行和管理编码代理的工具。对于需要在外出时监控 AI 编码进场的开发者来说，这是一个真正实用的功能。

### 代理编排创新
Skills 系统引入了代理间协作的新范式——handoff、loop、advisor、committee 四种模式让多个 AI 代理可以像人类团队一样分工协作。

### 开源 + 自托管
采用 AGPL-3.0 许可证，完全开源。支持自托管中继服务器，数据不经过第三方，满足安全和隐私需求。

### 快速启动
桌面应用下载后自动启动守护进程，手机端扫描 QR 码即可连接，零配置上手。

---

## 应用场景

### 远程编码监控
开发者在外出时通过手机查看编码代理的实时工作状态，发送指令或启动新任务，实现真正的"随时随地编程"。

### 多代理协作开发
使用 handoff 和 committee 等 Skills，让不同的 AI 编码代理负责不同模块，通过 Paseo 统一编排和调度。

### 团队编码管理
团队中多人通过 Paseo 共享代理状态，统一管理和监控团队的 AI 辅助编码工作流。

### 多环境切换
在不同设备间无缝切换——在桌面开始编码任务，在手机上检查进度，在 CLI 中执行自动化脚本。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| **总 Stars** | ~7,564 |
| **总 Forks** | ~714 |
| **许可证** | AGPL-3.0 |
| **创建时间** | 2025 年 10 月 13 日 |
| **主要语言** | TypeScript |

---

## 总结

Paseo 是一个定位独特的 AI 编码代理编排平台，通过本地守护进程 + 多端客户端的架构实现了"从任何设备运行编码代理"的目标。其代理编排 Skills 系统引入了多代理协作的新范式，移动端支持填补了 AI 编码工具在移动场景下的空白，是目前 AI 编码工具生态中极具创新性的项目。

---

*数据来源：GitHub 仓库 (getpaseo/paseo)，2026 年 6 月访问*
