# TaxHacker 项目分析

> **一句话总结：** TaxHacker 是一款开源、可自托管的 AI 记账应用，利用大语言模型（LLM）自动识别和提取发票、收据、银行对账单中的财务数据，帮助自由职业者和小型企业轻松实现自动化记账和报税准备。

---

## 一、基本信息

| 项目 | 详情 |
|---|---|
| **项目名称** | TaxHacker |
| **GitHub 地址** | https://github.com/vas3k/TaxHacker |
| **官方网站** | https://taxhacker.app |
| **Stars** | 4,385 |
| **Forks** | 695 |
| **Open Issues** | 41 |
| **Watchers** | 43 |
| **许可证** | MIT License |
| **主要语言** | TypeScript |
| **作者** | vas3k（https://github.com/vas3k） |
| **创建时间** | 2025-03-10 |
| **最近更新** | 2026-04-03 |
| **Topics 标签** | accounting, ai-analysis, currency-exchange, expenses, gemini, invoices, llm, ocr-recognition, ollama, openai, self-hosted, taxes |

---

## 二、解决什么问题

自由职业者、独立开发者和中小企业在日常经营中面临以下痛点：

1. **手动记账耗时费力**：每笔消费都需要手动录入日期、金额、商户名称、税额等信息，效率极低且容易出错。
2. **票据格式五花八门**：超市收据、餐厅账单、电子发票、银行对账单、手写单据——格式各异，难以统一管理。
3. **多币种/多语言处理困难**：跨国业务涉及多种货币和语言，手动换算汇率并追溯交易日期的历史汇率非常麻烦。
4. **报税准备复杂**：向会计师或税务顾问提交报告时，需要将分散的票据数据整理成结构化的表格。
5. **财务数据隐私担忧**：使用云端记账服务意味着敏感的财务数据存储在第三方服务器上，存在隐私泄露风险。
6. **现有工具不够灵活**：市面上的记账软件通常字段和分类固定，无法根据特定行业或国家需求进行定制。

TaxHacker 通过将 LLM（大语言模型）与结构化数据库相结合，提供了一套完整的解决方案：拍照上传票据即可自动提取所有关键数据，支持自定义字段和分类，可完全自托管以保障数据隐私。

---

## 三、核心功能

### 1. AI 智能票据识别与数据提取

- 拍照或上传收据、发票、银行对账单、PDF 等各种格式的文件
- AI 自动识别并提取关键信息：日期、金额、商户名称、商品明细、税额等
- 自动分类：根据票据内容将交易归入相应类别
- 商品拆分：从发票中逐项提取商品并拆分为独立交易
- 支持多语言、多币种文档的识别
- 可识别手写收据、信件等多种非标准文档

### 2. 多币种支持与自动汇率换算

- 自动检测文档中的币种
- 基于交易日期的历史汇率进行精确换算
- 支持全球 170+ 种法定货币
- 支持 14 种主流加密货币（BTC、ETH、LTC、DOT 等）
- 换算到用户设定的基础货币

### 3. 可自定义的分类、项目与字段体系

- 创建自定义分类和项目来组织交易
- 支持无限自定义字段（类似 Excel 中添加自定义列）
- 自定义字段可配合 AI 提示词从发票中提取特定信息
- 全文搜索：搜索已识别文档的实际内容
- 高级筛选：按日期范围、分类、项目、金额等条件过滤
- 批量操作：一次性处理多个文档或交易

### 4. 完全可定制的 LLM 提示词

- 可修改系统级提示词模板，适配特定业务场景
- 为特定字段或项目编写自定义提取规则
- 调整字段提取优先级和命名约定
- 针对特定行业文档优化 AI 理解能力
- 所有 AI 提取过程完全透明，可在设置中随时修改

### 5. 灵活的数据筛选与导出

- 按日期、分类、项目、金额及自定义字段进行高级筛选
- 导出为 CSV 格式，支持附带所有相关文档
- 生成可供会计师或税务顾问使用的报税报告
- 数据可移植：下载完整数据归档，随时迁移

### 6. 自托管与隐私保护

- Docker 原生支持，一键部署
- 数据完全存储在本地，不离开用户控制范围
- 无供应商锁定，随时导出全部数据
- 源代码完全开放，操作透明可审计

---

## 四、技术栈

### 前端与全栈框架
- **Next.js 15+**：基于 React 的全栈框架，负责前端页面和 API 路由
- **TypeScript**：项目主体语言（占比约 99%）

### 数据库
- **PostgreSQL 17+**：关系型数据库，存储所有结构化数据
- **Prisma**：ORM 框架，负责数据模型定义和数据库迁移

### AI / LLM 集成
- **OpenAI**（GPT 系列）：支持通过 API Key 接入
- **Google Gemini**：支持通过 API Key 接入
- **Mistral**：支持通过 API Key 接入
- **Ollama**（规划中）：本地 LLM 支持

### 文档处理
- **Ghostscript**：PDF 文件处理
- **GraphicsMagick**：图像处理与转换

### 部署
- **Docker**：容器化部署，提供官方 Docker 镜像
- **Docker Compose**：一键编排应用 + PostgreSQL
- **GitHub Container Registry (ghcr.io)**：Docker 镜像托管

### 认证
- **Better Auth**：用户认证框架，支持自托管模式下的自动登录

### 语言分布
| 语言 | 占比 |
|---|---|
| TypeScript | ~99% |
| CSS | 少量 |
| Dockerfile | 少量 |
| Shell | 少量 |
| JavaScript | 极少 |

---

## 五、使用场景

### 场景一：自由职业者的日常记账
自由职业者经常有大量零散的支出票据（餐饮、交通、办公用品等）。使用 TaxHacker，只需拍照上传收据，AI 会自动提取金额、日期、商户、类别等信息并存入数据库。月末或年末导出报告即可交付给会计师。

### 场景二：独立开发者的跨国业务管理
独立开发者在不同平台（Stripe、Patreon、App Store 等）有收入，支出也涉及多币种。TaxHacker 能自动识别不同币种并按历史汇率换算为基础货币，省去手动换算的麻烦。

### 场景三：小型企业的报税准备
小型企业可以将所有发票和收据上传到 TaxHacker，利用自定义分类和项目来组织支出（如"办公用品"、"差旅费"、"广告支出"等）。报税时按分类筛选并导出，直接提供给会计师。

### 场景四：加密货币投资者
支持 14 种主流加密货币的汇率换算，可以追踪加密货币相关的消费和投资支出，按交易日期自动获取当时的加密货币汇率。

### 场景五：注重隐私的财务管理者
对于不希望将敏感财务数据上传到第三方云服务的用户，TaxHacker 可以完全自托管在家用服务器或 NAS 上，所有数据只存储在本地。

### 场景六：特定行业的定制化记账
通过自定义字段和 AI 提示词，用户可以为特定行业创建专属的数据提取规则。例如，餐饮行业可以提取"餐桌号"、"服务员"等字段；物流行业可以提取"运单号"、"发货地"等信息。

---

## 六、部署方式

### Docker 一键部署（推荐）

```bash
curl -O https://raw.githubusercontent.com/vas3k/TaxHacker/main/docker-compose.yml
docker compose up
```

### 自定义 Docker 配置

```yaml
services:
  app:
    image: ghcr.io/vas3k/taxhacker:latest
    ports:
      - "7331:7331"
    environment:
      - SELF_HOSTED_MODE=true
      - UPLOAD_PATH=/app/data/uploads
      - DATABASE_URL=postgresql://postgres:postgres@localhost:5432/taxhacker
    volumes:
      - ./data:/app/data
    restart: unless-stopped
```

### 本地开发

```bash
git clone https://github.com/vas3k/TaxHacker.git
cd TaxHacker
npm install
cp .env.example .env
# 编辑 .env 配置数据库连接等
npx prisma generate && npx prisma migrate dev
npm run dev
```

访问 `http://localhost:7331` 即可使用。

---

## 七、项目评估

### 优势
- **AI 原生设计**：不是简单地在传统记账工具上"贴"AI，而是从底层就将 LLM 作为核心数据提取引擎
- **高度可定制**：从分类、字段到 AI 提示词，几乎所有元素都可以自定义
- **隐私优先**：自托管模式保证数据不离开用户控制范围
- **部署简单**：Docker 一键部署，开箱即用
- **多 LLM 支持**：不绑定单一 AI 提供商，支持 OpenAI、Gemini、Mistral，未来还将支持本地 LLM
- **MIT 许可证**：完全开源免费，商业使用无限制

### 局限
- 项目仍处于早期开发阶段（作者明确提醒 "Use at your own risk"）
- 目前不支持中文发票格式的专项优化（但 LLM 本身可处理多语言）
- 本地 LLM 支持尚未完成（Ollama 支持在路线图中）
- 暂无移动端 App，依赖 Web 浏览器访问

### 项目热度
- 从 2025 年 3 月创建至 2026 年 4 月，短短一年时间获得 4,385 Stars 和 695 Forks
- 项目活跃度高，最近一次提交在 2026 年 4 月 3 日
- 作者 vas3k 是 GitHub 上的知名开发者，拥有丰富的开源项目经验

---

> **一句话总结：** TaxHacker 是一款面向自由职业者和中小企业的开源 AI 记账工具，通过 LLM 自动提取票据数据、支持多币种换算和完全自托管，让记账和报税准备变得简单高效——把最繁琐的财务工作交给 AI，把数据控制权留给自己。
