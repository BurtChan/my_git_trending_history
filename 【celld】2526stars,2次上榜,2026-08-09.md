# celld 项目分析

## 项目名称
**celld** — 自托管、分布式的 Durable Objects 守护进程（Deno 官方出品）
- **GitHub**: [denoland/celld](https://github.com/denoland/celld)
- **许可证**: Apache-2.0

---

## 项目概述
celld 是 Deno 团队推出的开源守护进程，让你**在自己机器上运行 Cloudflare Workers 与 Durable Objects**。核心设计极其激进：每个对象就是一个独立的 SQLite 数据库，按名称寻址，并复制到你拥有的 S3 兼容存储桶中；节点之间仅通过该存储桶协调，**没有控制平面、没有共识协议**。

架构上，每个 celld 节点内嵌 V8 引擎执行 Wrangler 打包的 Worker 项目。对象存储的 compare-and-swap 操作保证同一时刻只有一个节点拥有某个 cell，无需成员协议、故障检测器或共识服务。cell 的 SQLite 数据库持续复制到存储桶，cell 迁移或唤醒时新所有者直接恢复数据库继续执行——存储桶是唯一的事实来源，节点随时可替换。空闲的 cell 可以休眠到近乎零资源占用。

项目创建于 2025 年 4 月，今日 +546 Star 首次登上 Trending（仅 3 个 commits 却引发热议，说明设计文档的冲击力）。安装器提供可验证的二进制来源（gh attestation verify）。

---

## 核心功能
| 功能 | 说明 |
|------|------|
| 自托管 Durable Objects | 在自己硬件上运行 DO 语义，无需 Cloudflare 云 |
| 对象即数据库 | 每个对象一个 SQLite 库，天然分片 |
| 无控制平面协调 | S3 存储桶 CAS + 所有权记录实现唯一归属 |
| 持续复制 | cell 状态实时复制到自有存储桶，节点可替换 |
| 休眠省资源 | 空闲 cell 近乎零占用 |
| Worker 兼容 | 直接执行 Wrangler 打包的 Cloudflare Worker 项目 |

---

## 技术栈
| 组件 | 技术 |
|------|------|
| 语言 | Rust |
| 运行时 | 内嵌 V8 |
| 存储 | SQLite（每对象一库）+ S3 兼容桶 |
| 部署 | Dockerfile、celld deploy（需 esbuild） |
| 兼容 | Cloudflare Workers / Wrangler bundles |

---

## 项目亮点

### 用"无共识"重新定义分布式
celld 证明分布式状态协调不一定需要 Raft/Paxos——对象存储的 CAS 原语 + 所有权记录即可实现"单写者"语义，架构极简而优雅。

### 按构造分片
每个对象自带数据库，应用天然分片：单一共享数据库的争用与爆炸半径问题在设计层面就被消除，而不是靠运维缓解。

### Deno 团队背书
Deno Land 出品（ry@deno.com 接受补丁），工程质量与长期维护有保障，Apache-2.0 许可宽松。

---

## 应用场景

### 自托管边缘应用
不想被云厂商锁定的团队，用自己机房或 VPS 运行具备 DO 语义的应用。

### 数据主权敏感业务
SQLite 数据库复制到自有 S3 桶，数据完全自主可控，满足合规要求。

### 分布式应用教学与研究
"无控制平面的分布式协调"是极具启发性的架构样本，适合架构师研究。

---

## Star 数据
| 指标 | 数值 |
|------|------|
| ⭐ 总 Stars | 2,526 |
| 🍴 总 Forks | 71 |
| 今日新增 | +563 |

---

---

## 📋 更新记录

### 更新 1 — 2026 年 8 月 9 日（再次登上 Trending）
**更新原因**：项目再次登上 GitHub Trending 榜单（563 stars today）

**最新动态**：
- Star 数从 1,963 增长至 2,526（+563），Fork 数从 52 增长至 71（+19），累计增长近 29%，热度持续攀升
- 自 8 月 7 日首次上榜后，celld 在 Reddit/HN 的架构讨论持续发酵，「无控制平面 + S3 CAS 协调」的设计引发分布式系统领域广泛讨论
- Deno 团队持续完善安装器与文档，社区围绕自托管边缘场景的用例分享增多，项目进入快速成长期

**最新 Star 数据**：

| 指标 | 上次记录 | 最新数据 | 变化 |
|------|----------|----------|------|
| 总 Stars | 1,963 | 2,526 | +563 |
| 总 Forks | 52 | 71 | +19 |

**核心变化概要**：
- 两天内 Star 增长 +563，增速显著（首次上榜仅 1,963）
- 架构讨论从「概念验证」转向「生产自托管实践」
- Deno 团队持续投入，社区用例逐渐丰富

---

## 总结
celld 用"每个对象一个 SQLite + S3 CAS 协调"的极简设计，把 Cloudflare Durable Objects 带到了自托管环境，无控制平面、无共识的架构理念在分布式系统领域极具话题性，上线即引发热议。

---

*数据来源：GitHub 仓库 (denoland/celld)，2026 年 8 月访问*
*首次分析：2026 年 8 月 7 日 | 最近更新：2026 年 8 月*
