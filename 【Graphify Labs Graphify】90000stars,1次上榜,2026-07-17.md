# Graphify (Graphify-Labs) 项目分析

## 项目名称
**Graphify** — AI 编程助手知识图谱技能插件，将代码、文档、数据库等自动映射为可查询的知识图谱
- **GitHub**: [Graphify-Labs/graphify](https://github.com/Graphify-Labs/graphify)
- **许可证**: MIT
- **语言**: Python
- **Star**: 89,278 | **Fork**: 8,714 | **今日新增**: 1,107

---

## 项目概述

Graphify 是一个面向 AI 编程助手（Claude Code、Codex、OpenCode、Cursor、Gemini CLI 等 20+ 平台）的知识图谱构建技能插件。用户只需在编程助手中输入 `/graphify`，即可将整个项目——包括代码、SQL Schema、Shell 脚本、文档、PDF、图片、视频等——自动映射为可查询的知识图谱，替代传统的文件搜索和 grep 操作。

该项目的核心理念是"代码解析完全本地化，零 LLM 消耗"。代码文件通过 tree-sitter AST 解析器进行确定性语法分析，生成函数调用、模块导入、类继承等关系边，整个过程中不调用任何大语言模型，也不将代码发送到远程服务器。文档、PDF、图片和视频等非代码内容则通过用户配置的 API 进行语义解析，同样映射到同一个知识图谱中。

Graphify 由 Y Combinator S26 批次孵化，自 2026 年 4 月创建以来在极短时间内获得了近 9 万 Star，成为 AI 编程工具生态中最热门的技能插件之一。

---

## 核心功能

| 功能 | 描述 |
|------|------|
| **多语言代码解析** | 支持 40+ 种编程语言，通过 tree-sitter 提取函数调用、导入、继承、混入等跨文件关系 |
| **知识图谱构建** | 自动构建项目级别的知识图谱，包含 God nodes（最连接的概念）、社区发现（Leiden 算法）、跨文件链接 |
| **交互式可视化** | 生成 `graph.html` 可在浏览器中打开，支持节点点击、过滤和搜索 |
| **多格式支持** | 除代码外还支持文档、PDF、图片、视频/音频映射到同一图谱 |
| **20+ 平台适配** | 支持 Claude Code、Cursor、Codex、Gemini CLI、GitHub Copilot CLI、Aider、OpenCode 等主流 AI 编程助手 |
| **Always-On 模式** | 安装后可配置为始终启用，AI 助手每次搜索前自动先查询知识图谱 |
| **查询与路径追踪** | 支持自然语言查询、路径追踪、概念解释等操作 |
| **MCP 服务器** | 可选安装为 MCP stdio 服务器，供外部工具调用 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 代码解析 | tree-sitter AST（40+ 语言支持） |
| 知识图谱存储 | graph.json（本地 JSON 格式） |
| 社区发现 | Leiden 算法 |
| 语义解析 | 可选 Ollama（本地）/ OpenAI / Claude API / Bedrock |
| 数据库支持 | SQL Schema 提取、PostgreSQL 实时内省 |
| 基础设施支持 | Terraform/HCL 提取 |
| 可视化 | HTML 交互式图谱 |
| 包管理 | uv / pipx（PyPI 包名为 `graphifyy`） |

---

## 项目亮点

### 零 LLM 消耗的代码解析

Graphify 的代码解析完全基于 tree-sitter 的确定性 AST 分析，不依赖大语言模型。这意味着构建代码知识图谱不消耗任何 token，成本为零。解析结果完全可重复、可预测，不会因为 LLM 的随机性导致图谱不稳定。这一设计在 LLM 成本敏感的开发场景中具有显著优势。

### 精准的 Benchmark 表现

在标准评测中，Graphify 表现出色：在 LOCOMO（n=300）评测中 recall@10 达到 0.497，远超 mem0 的 0.048 和 supermemory 的 0.149；在 LongMemEval-S（n=50）中 QA 准确率 76%，与 dense RAG 持平。更重要的是，Judge 盲评显示与第二位裁判的一致性达 90.6%（Cohen's kappa 0.81），证明了评测的可靠性。

### 边缘标注机制

图谱中每条关系边都被明确标注为 `EXTRACTED`（从源代码中直接提取）或 `INFERRED`（由 graphify 推断得出），使用户可以清晰区分哪些关系是代码中明确存在的，哪些是系统推导的，增强了结果的可信度和可审计性。

### YC 背景与生态覆盖

作为 YC S26 孵化项目，Graphify 已适配超过 20 个 AI 编程助手平台，几乎覆盖了当前所有主流工具。其"Always-On"模式通过 Hook 机制（Claude Code、Gemini CLI）或持久化指令文件（Codex、Cursor）实现，让知识图谱查询成为 AI 助手的默认行为，大幅提升了开发效率。

---

## 应用场景

### 大型代码库导航

在拥有数千文件的大型项目中，开发者经常需要理解模块间的依赖关系和调用链。Graphify 将整个代码库映射为知识图谱，开发者只需问"哪些模块调用了认证服务"或"从用户请求到数据库写入的完整链路是什么"，即可获得精确答案，无需手动 grep 多个文件。

### 代码审查与重构

通过知识图谱的 God nodes（核心概念节点）和社区发现功能，审查者可以快速识别项目中的核心模块、耦合关系和潜在的重构目标。跨文件的 calls/imports/inherits 关系一目了然，帮助团队做出更明智的架构决策。

### 新成员快速上手

新加入项目的开发者可以通过 `GRAPH_REPORT.md`（自动生成的高亮报告）快速了解项目结构、关键概念和模块间关系，大幅缩短熟悉项目的时间。报告包含关键概念、意外发现和推荐问题，是天然的 onboarding 材料。

### 多格式知识整合

对于文档密集型项目（如包含大量 RFC、设计文档、架构图），Graphify 可以将代码、文档、PDF、图片甚至视频映射到同一个知识图谱中，实现真正的"全项目知识统一查询"。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| 总 Star | 89,278 |
| 总 Fork | 8,714 |
| 今日新增 | 1,107 |
| 创建时间 | 2026-04-03 |
| 主要语言 | Python |
| 许可证 | MIT |

---

## 总结

Graphify 是一个设计精良的 AI 编程助手技能插件，通过 tree-sitter AST 实现零成本、确定性的代码知识图谱构建，并扩展到文档、PDF、图片、视频等多格式支持。其 20+ 平台的广泛适配、"Always-On" 模式的无缝集成，以及出色的 benchmark 表现，使其成为 AI 辅助编程领域知识管理方向的最强工具之一。对于需要理解和导航大型代码库的开发者来说，Graphify 提供了一种全新的、更高效的交互方式。

---

*数据来源：GitHub 仓库 (Graphify-Labs/graphify)，2026 年 7 月访问*
