# Andrej Karpathy Skills 项目分析

> **一句话总结** — 将 Andrej Karpathy 对 LLM 编程缺陷的洞察提炼为四条可操作的编码准则，以单个 CLAUDE.md 文件改善 AI 编程助手行为。

- **GitHub**: [forrestchang/andrej-karpathy-skills](https://github.com/forrestchang/andrej-karpathy-skills)
- **语言**: Markdown（纯配置/指导文件，无编程语言）
- **Stars**: ~7,700+ | **Forks**: ~547
- **许可证**: MIT
- **创建时间**: 2026-01-27

---

## 解决什么问题

Andrej Karpathy（前 Tesla AI 总监、OpenAI 联合创始人）在社交媒体上公开指出了当前 LLM 辅助编程中普遍存在的三类痛点：

1. **隐性假设与困惑管理失败** — 模型会代替用户做出错误假设并一路执行下去，不澄清、不质疑、不呈现权衡取舍。
2. **过度工程化** — 倾向于将本应 100 行解决的代码膨胀到 1000 行，制造不必要的抽象层和"灵活性"。
3. **连带破坏** — 在执行任务时顺手删除或修改不相关的注释、代码，造成意料之外的副作用。

这些问题的本质是：LLM 缺乏"停顿思考"的习惯，倾向于"先动手再后悔"。本项目将 Karpathy 的洞察系统化为一套可集成到 Claude Code（以及类似 AI 编程工具）中的行为准则，直接嵌入 `CLAUDE.md` 配置文件，从根源上约束 LLM 的编码行为模式。

## 核心功能

项目围绕 **四条核心原则** 构建，每条原则精准对应一类 LLM 编程缺陷：

### 原则一：先思考再编码（Think Before Coding）
- **目标**: 消除隐性假设和困惑
- **要求**: 显式陈述假设、呈现多种理解而非沉默选择、适时反驳、遇到困惑主动停下提问
- **核心精神**: "不要假设，不要隐藏困惑，把权衡摆到台面上"

### 原则二：简洁优先（Simplicity First）
- **目标**: 对抗过度工程化倾向
- **要求**: 只写解决问题所需的最少代码；不做未请求的功能、不为单次使用创建抽象、不预防不可能发生的错误；200 行能缩减为 50 行就必须重写
- **检验标准**: "一个资深工程师会认为这段代码过于复杂吗？如果是，简化它"

### 原则三：精准手术式修改（Surgical Changes）
- **目标**: 防止连带破坏
- **要求**: 只触碰必须修改的部分；不"顺手改进"相邻代码、注释或格式；匹配已有代码风格；发现无关死代码只提示不删除；只清理自己制造的无用残留
- **检验标准**: "每一行变更都应该能追溯到用户的原始请求"

### 原则四：目标驱动执行（Goal-Driven Execution）
- **目标**: 利用 LLM 擅长循环验证的特性
- **要求**: 将命令式任务转化为可验证的目标（如"修复 bug" → "先写复现测试，再让它通过"）；多步骤任务需列出验证计划
- **核心洞察**: 源自 Karpathy 原话 — "LLM 非常擅长循环直到达成特定目标...不要告诉它做什么，给它成功标准，然后看着它发挥"

### 配套示例文档（EXAMPLES.md）
项目还提供了一份详细的反模式示例文档，包含 8 个真实场景对比（错误示范 vs 正确做法），覆盖隐藏假设、多重解读、过度抽象、投机功能、顺带重构、风格漂移、模糊目标、缺少验证等典型问题。

## 技术栈

本项目的技术栈极为精简，本质是一个 **纯文本配置项目**：

| 组件 | 说明 |
|------|------|
| `CLAUDE.md` | 核心行为准则文件（~2.4KB），可直接嵌入任何项目 |
| `EXAMPLES.md` | 反模式示例文档（~14.8KB），以 Python 代码对比展示 |
| `.claude-plugin/` | Claude Code 插件配置目录，支持 Skills CLI 安装 |
| `skills/` | Skill 定义目录 |

**安装方式**：
- **Skills CLI**（推荐）: `npx skills add forrestchang/andrej-karpathy-skills`
- **手动下载**: 通过 curl 将 CLAUDE.md 下载或追加到项目根目录

**兼容性**: 主要面向 Claude Code，但其准则具有通用性，可适配任何接受 system prompt / 自定义指令的 AI 编程工具（Cursor、Windsurf、Copilot Chat 等）。

## 使用场景

1. **日常 AI 辅助编程** — 将 CLAUDE.md 放入项目根目录，让 Claude Code 在每次交互中自动遵循这些准则，显著减少代码审查回合
2. **团队编码规范** — 作为团队共享的 AI 协作基线，统一所有成员使用 AI 编程助手时的行为预期，减少"AI 写了一堆不需要的东西"的抱怨
3. **LLM 行为研究** — 作为研究 LLM 编程行为的参考框架，四条原则精准归纳了当前 LLM 的典型缺陷模式
4. **AI 编程工具评测基准** — 可用于对比不同 AI 编程工具在"简洁性"、"精准修改"、"假设管理"等维度上的表现
5. **教学材料** — EXAMPLES.md 中的正反对比是极好的 AI 辅助编程最佳实践教材，适合用于培训开发者如何更好地与 AI 协作
6. **项目模板初始化** — 新项目启动时直接引入，从第一天就建立健康的 AI 编程习惯

## 一句话总结

> 将 Andrej Karpathy 对 LLM 编程缺陷的深刻洞察（隐性假设、过度工程、连带破坏），系统化为四条简洁可操作的行为准则，以单个 Markdown 文件的形式嵌入 AI 编程工具，用最小成本显著提升 AI 辅助编程的代码质量。这不是一个代码库，而是一份"AI 编程行为宪法"。
