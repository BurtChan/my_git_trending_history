# 一个好的面试题：计算中位数 项目分析

## 文章名称

**一个好的面试题：计算中位数** — 为什么"计算数组中位数"是比 Fizz Buzz 更好的面试题

- **来源**: https://krisshamloo.com/blog/007
- **作者**: Kris Shamloo

---

## 文章概述

Kris Shamloo 的这篇文章探讨了面试题设计的艺术——为什么一道看似简单得不起眼的"写一个函数计算数组中位数"实际上是一个优秀的编程面试题。文章的核心论点是：一个好的面试题应该像洋葱一样有层次——最外层足够简单让任何人都能动手写，但往里挖每一层都能引出更深层的技术讨论。Fizz Buzz 太简单会侮辱优秀候选人，而 LeetCode 硬题又只能考察算法竞赛选手。"计算中位数"恰好处于这两个极端之间的最佳平衡点。

文章从最基本的问题定义开始展开：中位数是什么？对于已排序数组，如果长度为奇数，中位数是中间那个元素；如果长度为偶数，中位数是中间两个元素的平均值。这个简单的定义立刻引出了第一个设计决策：函数应该接受已排序的数组还是未排序的数组？如果接受未排序数组，函数内部是否应该先排序？是否应该修改原始数组还是创建副本？这些 API 设计问题虽然不涉及复杂算法，但直接反映了候选人的工程思维成熟度。

接着文章深入到实现层面的多个技术维度。Off-by-one 错误是中位数计算中最常见的陷阱——数组索引从 0 开始，长度为 n 的数组中间位置是 n/2 还是 (n-1)/2？奇数和偶数长度的处理逻辑如何统一？这些边界条件看似琐碎，但在面试环境下是区分细心程度和代码质量意识的关键信号。一个能主动讨论这些边界情况的候选人，通常在实际工作中也能写出更健壮的代码。

更深层的技术讨论围绕性能和算法选择展开。排序的时间复杂度是 O(n log n)，但计算中位数真的需要完整排序吗？文章引入了 Quickselect 算法——一种可以在 O(n) 平均时间复杂度内找到第 k 小元素的算法。这一知识点的引入将面试从"基础编程能力"层面提升到了"算法素养"层面。候选人是否知道 Quickselect、能否解释其工作原理、是否理解最坏情况下的退化（O(n²)），都是非常有价值的技术信号。

文章还从统计学的角度拓展讨论：为什么中位数比均值更适合描述偏态分布的数据？在什么场景下应该用中位数而非均值？这个问题能够考察候选人是否理解数据的统计特性，以及是否能在实际业务场景中选择合适的统计方法。Python 的 statistics 模块提供了内置的 median 函数，候选人是否知道标准库的存在，也是了解其语言熟悉程度的一个维度。

整个面试过程可以按照候选人的水平自然分层展开：基础层（能写出正确的排序+索引实现）→ 设计层（讨论 API 设计、是否修改原数组、性能权衡）→ 算法层（Quickselect 及其时间复杂度分析）→ 统计层（中位数的统计学意义和应用场景）。这种自然分层的能力，是这道题作为面试题最大的优势——它不会放过优秀候选人，也不会让基础薄弱的候选人无话可说。

---

## 核心内容

### 面试题分层展开框架

| 层级 | 讨论维度 | 考察的能力 | 预期时间 |
|------|---------|-----------|---------|
| **第一层** | 基本实现（排序 + 索引） | 编码基本功、语法熟悉度 | 5 分钟 |
| **第二层** | API 设计（是否修改原数组、参数类型） | 工程思维、API 设计意识 | 5 分钟 |
| **第三层** | 边界条件（空数组、单元素、奇偶分支） | 健壮性思维、off-by-one 意识 | 5 分钟 |
| **第四层** | 性能优化（Quickselect O(n)） | 算法素养、复杂度分析 | 10 分钟 |
| **第五层** | 统计意义（中位数 vs 均值） | 领域知识、实际应用理解 | 5 分钟 |

### 关键技术要点

- **中位数定义**：奇数长度取中间元素，偶数长度取中间两元素的平均值
- **Off-by-one 陷阱**：索引从 0 开始，中间位置计算需要区分奇偶
- **排序决策**：函数是否应该内部排序？O(n log n) 的排序开销是否可接受？
- **原地排序 vs 创建副本**：是否修改原数组是 API 设计的关键决策
- **Quickselect 算法**：基于 partition 的选择算法，平均 O(n)，最坏 O(n²)
- **Python statistics 模块**：`statistics.median()` 提供了内置实现
- **统计意义**：中位数对异常值鲁棒，均值对异常值敏感

### 中位数 vs 均值对比

| 特性 | 中位数 | 均值 |
|------|--------|------|
| **计算复杂度** | O(n log n) 排序或 O(n) Quickselect | O(n) 一次遍历 |
| **对异常值的鲁棒性** | 强（不受极端值影响） | 弱（被极端值拉偏） |
| **适用分布** | 偏态分布（收入、房价等） | 正态分布或对称分布 |
| **可加性** | 无（不能分组计算后合并） | 有（可以分组计算后加权合并） |

---

## 文章亮点

1. **"洋葱型"面试题的精确定义**：文章清晰地阐述了好面试题的标准——像洋葱一样有层次，从简单到复杂自然过渡。这个比喻非常精确：最外层是任何人都能回答的基本问题，剥开一层是设计考量，再剥一层是算法深度，核心是领域知识。这种"自然分层"的特性让面试官能在一个问题框架内准确评估不同水平候选人的能力边界。

2. **超越 Fizz Buzz 的方法论论证**：文章明确指出 Fizz Buzz 的两个致命缺陷——太简单会侮辱优秀候选人、信号模糊（能通过不代表什么）。而"计算中位数"在难度上恰好处于"不会侮辱任何人但足够引出深入讨论"的甜蜜点。这种对面试题难度的精准把控，体现了作者对面试设计的深入思考。

3. **API 设计讨论的技术深度**：文章精彩之处在于，把一个看似纯算法的问题引导到 API 设计层面——"函数应该排序输入数组吗？"这个问题看似无关紧要，实际上触及了函数设计的基本原则：函数是否应该有副作用？调用者是否期望输入被修改？性能优化是否应该以 API 简洁性为代价？这些讨论能极其有效地考察候选人的工程判断力。

4. **Quickselect 的自然引入方式**：文章不是生硬地要求候选人实现 Quickselect，而是通过"排序真的是最优解吗？"这个问题自然引出。这种引导方式既尊重了基础扎实的候选人（他们可能直接想到了 Quickselect），也给一般候选人留下了思考空间（从 O(n log n) 到 O(n) 的优化空间是显然的）。这种自然引导的面试技巧本身就是文章的重要价值。

5. **统计维度拓展了面试边界**：将讨论从中位数计算延伸到"中位数为什么比均值更好"的统计问题，是这篇文章最有深度的部分。这把一个纯粹的编程问题变成了一个跨领域讨论——候选人需要同时具备编程能力、算法知识和统计素养。这种多维度考察的方式，比任何单一维度的面试题都更能全面评估候选人的综合能力。

---

## 适用场景

1. **技术面试的题目设计参考**：如果你是一名面试官或技术管理者，这篇文章提供了一个可以直接使用的面试题模板。"计算中位数"这道题适用于大多数编程语言，可以从基础到高级自然展开，是评估初中级到中级工程师的理想题目。

2. **面试准备策略指导**：对于正在准备技术面试的候选人，这篇文章揭示了一个重要策略：准备面试不是准备标准答案，而是理解每个问题可能展开的多个讨论维度。即使是一道看似简单的"计算中位数"，也涉及 API 设计、边界条件、算法优化和统计意义等多个层面。

3. **编程教学中的问题设计**：对于编程教师或技术培训师，这篇文章展示了一种优秀的教学问题设计方法——从简单问题出发，通过层层递进的引导，让学生在解决具体问题的过程中自然掌握更广泛的知识。"计算中位数"可以作为一个很好的课程设计范例。

4. **团队代码评审中的讨论框架**：文章中对 API 设计（是否修改原数组、参数类型设计）的讨论，不仅适用于面试，也适用于日常的代码评审。这些设计考量为团队制定编码规范提供了具体的讨论素材。

---

## 总结

Kris Shamloo 的这篇文章表面上在讨论一道面试题，实际上在阐述一套完整的面试设计哲学。"计算中位数"这道题的精妙之处在于它的多维度可展开性——从 5 分钟的基础编码到 30 分钟的深度技术讨论，都能在同一道题的框架内自然进行。这不是那种需要候选人背诵答案的 puzzle 题，也不是那种考察特定语言冷知识的偏门题，而是一道真正能考察工程思维广度和深度的"好问题"。

文章最有价值的洞见是：好的面试题不是一个静态的标准答案，而是一个动态的探索过程。面试官通过同一道题的不同提问角度，可以自然地调整考察的深度和广度，既不会让初级候选人感到挫败，也不会让高级候选人觉得无聊。这种"以一题探多层"的面试设计思路，值得每一位参与技术面试的工程师学习和借鉴。

---

*数据来源：https://krisshamloo.com/blog/007，分析日期 2026年7月10日*
