# G0DM0D3 项目分析

## 项目名称
**G0DM0D3** — 全开源多模型 AI 聊天界面，专为红队测试、认知研究和自由 AI 交互打造
- **GitHub**: [elder-plinius/G0DM0D3](https://github.com/elder-plinius/G0DM0D3)
- **许可证**: AGPL-3.0

---

## 项目概述

G0DM0D3（读音 God Mode）是一个完全开源、隐私透明的多模型聊天界面，其核心使命是突破 AI 后训练层的限制，为红队测试、认知研究和自由 AI 交互提供强大工具。项目由 Elder Plinius 创建并维护，以「LIBERATED AI CHAT」为定位，面向黑客、哲学家和系统极客群体。

与传统单一模型聊天界面不同，G0DM0D3 的核心创新在于**多模型并行竞赛**机制。用户提问时，系统同时将请求发送给多个 AI 模型，各模型独立生成回答，然后通过评分系统自动评选最佳回复。这种「AI 竞技场」范式彻底改变了用户与 AI 交互的方式——从"信任一个 AI 是否正确"转变为"判断哪个 AI 最正确"。

项目采用单文件架构（`index.html`），无需任何构建步骤、框架依赖或 npm 安装。用户克隆仓库后直接在浏览器中打开即可使用，也可以部署到任何静态托管平台。这种极致简化的部署方式与项目功能的丰富程度形成了鲜明对比——表面上是一个简单的 HTML 文件，内部却集成了 60+ 个模型的支持、33 种输入扰动技术和 5 级 ULTRAPLINIAN 评估体系。

项目在 AI 安全研究社区引起了广泛讨论，被研究者用于探测模型行为边界、评估 AI 对齐效果以及进行跨模型对比分析。其内置的 Parseltongue 输入扰动引擎提供了系统化的红队测试能力，而隐私优先的架构设计确保用户数据不被追踪或泄露。

---

## 核心功能

| 功能 | 描述 |
|------|------|
| **GODMODE CLASSIC** | 5 个并行模型+提示组合竞赛，自动评选最佳回答 |
| **ULTRAPLINIAN 多层评估** | 5 个等级（12-60 个模型）的跨模型评估体系，100 分综合评分 |
| **Parseltongue 引擎** | 33 种输入扰动技术，3 个强度等级，用于红队测试和研究 |
| **多模型提供商** | 支持 60 个 OpenRouter 模型、44 个 Venice 模型和本地模型 |
| **本地模型支持** | 通过 Ollama、LM Studio、llama.cpp、vLLM 运行本地模型 |
| **AutoTune** | 20 种查询上下文下的自适应采样参数调整 |
| **隐私控制** | 元数据遥测可禁用，支持 No-Log 模式和纯本地模式 |
| **单文件架构** | 全部 UI 集成在单个 index.html 中，零依赖部署 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 前端 UI | 纯 HTML + CSS + JavaScript（单文件） |
| 模型调用 | OpenRouter API、Venice API、本地模型 API |
| 遥测管道 | Cloudflare Pages Functions（可选部署） |
| 数据存储 | 浏览器 localStorage（无云同步） |
| 许可证 | AGPL-3.0 |

---

## 项目亮点

### 多模型并行竞赛范式

G0DM0D3 的 GODMODE CLASSIC 模式同时运行 5 个不同的模型+策略组合（如 Claude Sonnet 4.6 + 边界反转、Grok 4.5 + 无过滤模式、Gemini 2.5 Flash + 拒绝反转等），让模型之间相互竞争而非依赖单一模型输出。这种范式让用户能够直观比较不同模型的能力边界和响应风格，对于 AI 研究者和开发者具有极高的参考价值。

### Parseltongue 输入扰动引擎

项目内置的 Parseltongue 引擎提供了 33 种结构化输入变换技术，覆盖 LeetSpeak、气泡文字、盲文、摩尔斯电码、Unicode 替换、音素变换、分层编码等多种方法。这些技术按 3 个强度等级组织，能够系统性地探测 AI 模型的鲁棒性和安全防护能力，是 AI 安全研究领域的实用工具。

### 隐私优先架构

G0DM0D3 的隐私设计非常彻底：聊天记录和设置仅存储在浏览器 localStorage 中，不创建账户、不同步云端。API 密钥直接从浏览器发送到模型提供商，不经过 G0DM0D3 的服务器。默认开启的遥测仅收集元数据（会话 ID、时间戳、模型/模式/层级、评分等），不包含提示文本、响应内容或 API 密钥，且可通过 No-Log 模式完全禁用。

### 极致简化的部署体验

整个项目的 UI 核心就是一个 `index.html` 文件——无需 Node.js、无需构建工具、无需 npm。克隆仓库后直接在浏览器中打开即可使用，也可以部署到 GitHub Pages、Vercel、Cloudflare Pages 等任何静态托管平台。这种零门槛的部署方式极大地降低了使用门槛，同时不影响功能的丰富性。

---

## 应用场景

### AI 安全研究与红队测试

安全研究者和红队测试人员可以使用 Parseltongue 引擎的 33 种输入扰动技术系统性地探测 AI 模型的安全边界，评估模型对齐效果和鲁棒性。ULTRAPLINIAN 的多层评估体系可以同时对数十个模型进行对比分析，发现不同模型在安全防护方面的差异。

### 跨模型能力对比与评估

开发者和研究者在选择 AI 模型时，可以使用 G0DM0D3 的多模型并行功能直接对比不同模型对同一问题的回答质量。ULTRAPLINIAN 的 100 分综合评分系统提供了量化的评估基准，帮助用户做出更明智的模型选择决策。

### AI 认知与行为研究

学术界和工业界的研究者可以利用 G0DM0D3 探究 AI 模型的认知边界、推理模式和行为特征。通过 AutoTune 的自适应采样参数和多种提示策略组合，研究者可以观察模型在不同条件下的行为变化，为 AI 认知科学研究提供实验平台。

### 隐私敏感的个人 AI 助手

注重隐私的用户可以使用 G0DM0D3 的纯本地模式，结合 Ollama 等本地模型运行框架，实现完全离线的 AI 对话体验。所有数据存储在本地浏览器中，不涉及任何云端传输，满足对隐私有极高要求的用户需求。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| 总 Stars | 9,450 |
| 总 Forks | ~2,200 |
| 今日新增 | +63 |
| 许可证 | AGPL-3.0 |
| 语言 | TypeScript |
| 创建时间 | 2026 年 |

---

## 总结

G0DM0D3 是一个定位独特的多模型 AI 交互平台——它不追求做"最好用的 AI 聊天工具"，而是将多个 AI 模型变成竞技场中的选手，让用户通过并行对比和结构化扰动来探索 AI 的真实能力边界。其单文件零依赖的部署方式、系统化的红队测试工具链和隐私优先的设计理念，使其在 AI 安全研究社区中独树一帜。

---

*数据来源：GitHub 仓库 (elder-plinius/G0DM0D3)，2026 年 7 月访问*
