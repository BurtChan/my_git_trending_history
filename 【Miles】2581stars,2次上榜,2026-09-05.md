# Miles 项目分析

## 项目名称
**Miles** — 面向企业级的大模型后训练强化学习框架（SGLang + Megatron-LM）
- **GitHub**: [radixark/miles](https://github.com/radixark/miles)
- **许可证**: Apache-2.0

---

## 项目概述
Miles 是由 radixark 维护（与 LMSYS/SGLang 社区深度协作）的高性能强化学习框架，专注 LLM/VLM 的大规模后训练（post-training）。它由 THUDM 的 slime 框架 fork 而来并共同演进，核心组合是 **SGLang 负责高吞吐 rollout + Megatron-LM 负责可扩展训练**，同时提供 PyTorch FSDP2 后端供偏好 HuggingFace 原生实现的场景。

项目的口号是「A journey of a thousand miles begins with a single rollout」，定位为万亿参数规模 RL 训练的企业级方案。2026 年 8 月发布 v0.1 正式版（官方博客《Miles v0.1: Production-level Post-training》），近期动态密集：Blackwell 原生 MXFP8/NVFP4 低精度 RL、Kimi K3 与 DeepSeek-V4 发布当天即支持（day-0）、AMD MI355X ROCm 支持、策略内蒸馏（on-policy distillation）等。2025 年 10 月创建，1,818 次提交，迭代极快。

---

## 核心功能
| 功能 | 说明 |
|------|------|
| 全异步 RL | rollout 与训练 worker 解耦，可配置 on/off-policy 调度，流水线气泡最小化 |
| 快速 Agentic Rollout | SGLang 引擎集群 + 路由分发，为多轮智能体负载调优 |
| 秒级权重更新 | 万亿参数模型（如 Kimi-K2.6）训练循环内秒级下发，P2P RDMA 快速路径 |
| 低精度训练 | MXFP8/NVFP4 数值稳定 RL 配方，另支持 FP8/INT4 QAT/BF16/FP16 |
| LoRA / 多 LoRA | 小规模 GPU 训练前沿模型，适配器直接加载进 SGLang |
| TITO | token-in-token-out，rollout 与训练间无 detokenize/retokenize 往返 |
| R3 路由重放 | MoE 专家路由在训练器前向中重放，消除路由失配导致的大规模不稳定 |
| 故障容忍 | SGLang 引擎崩溃后原地恢复续训，无需重启暂停 |

---

## 技术栈
| 组件 | 技术 |
|------|------|
| 训练后端 | Megatron-LM（主力）/ PyTorch FSDP2 |
| 推理引擎 | SGLang（含 sglang-diffusion） |
| 硬件支持 | NVIDIA GB300/GB200/B300/B200/H200/H100/A100；AMD MI300X/MI325/MI350/MI355X (ROCm) |
| 算法 | GRPO、GSPO、PPO、REINFORCE++、SFT、策略内蒸馏 |
| Day-0 模型 | DeepSeek-V4、Kimi-K3、GLM-5.2、Inkling、Nemotron 3 Ultra |

---

## 项目亮点
### 万亿参数级工程成熟度
P2P 权重传输、R3 路由重放、引擎级故障恢复三项特性直指大规模 RL 训练最痛的稳定性问题，是「生产级」定位的实证。

### 前沿模型 Day-0 支持
DeepSeek-V4、Kimi K3 等模型发布当天即可在其上跑 RL 训练，依托与 SGLang 团队的紧密协作，跟进速度业内罕见。

### Blackwell 原生低精度 RL
端到端 MXFP8/NVFP4 RL 训练在数值稳定配方下落地，直接降低新一代 GPU 集群的训练成本。

### 智能体环境生态
内置 Harbor、HUD、NeMo Gym、OpenEnv、Verifiers 等环境连接器，任务沙箱支持 AgentENV、Daytona、E2B、Modal，可训练编码与计算机操作智能体。

---

## 应用场景
### 大模型厂商后训练
为数十亿到万亿参数模型跑 GRPO/PPO 类 RL 对齐与推理强化训练的生产级框架。

### 多模态与扩散模型 RL
姊妹项目 miles_diffusion 提供 Flow-GRPO、DiffusionNFT 与 SFT，覆盖扩散模型的后训练。

### 企业智能体训练
通过环境连接器把编码/计算机操作智能体放进可复现的 RL 训练管线。

---

## Star 数据
| 指标 | 数值 |
|------|------|
| 总 Stars | 2,581 |
| 总 Forks | 446 |
| 今日新增 | 64 |
| 创建时间 | 2025-10-09 |

---

## 📋 更新记录

### 更新 1 — 2026 年 9 月 5 日（再次登上 Trending）
**更新原因**：项目再次登上 GitHub Trending 榜单（64 stars today）

**最新动态**：连续第二日在榜，Star 数从 2,451 增至 2,581（+130），Forks 从 437 增至 446（+9）。作为 SGLang + Megatron-LM 双引擎的 RL 后训练框架，Miles 在万亿参数稳定性工程上的定位持续吸引基础设施团队关注，稳步积累早期社区。

**最新 Star 数据**：

| 指标 | 上次记录 | 最新数据 | 变化 |
|------|----------|----------|------|
| 总 Stars | 2,451 | 2,581 | +130 |
| 总 Forks | 437 | 446 | +9 |

**核心变化概要**：
- 连续第二日在榜，Star +130、Forks +9，早期社区稳步扩张

---

## 总结
SGLang + Megatron-LM 双引擎驱动的企业级大模型 RL 后训练框架，以万亿参数规模的稳定性工程（P2P 权重传输、R3、故障恢复）和前沿模型 Day-0 支持为核心竞争力，是开源 RL 训练基础设施的第一梯队项目。

---

*数据来源：GitHub 仓库 (radixark/miles)，2026 年 9 月访问*
*首次分析：2026 年 9 月 | 最近更新：2026 年 9 月 5 日*
