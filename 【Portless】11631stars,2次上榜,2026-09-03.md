# Portless 项目分析

## 项目名称
**Portless** — 用稳定的命名 .localhost URL 替换端口号：给人类和 Agent 的本地开发体验升级
- **GitHub**: [vercel-labs/portless](https://github.com/vercel-labs/portless)
- **许可证**: Apache-2.0
- **语言**: TypeScript
- **主题**: developer-tools、localhost、proxy、monorepo

---

## 项目概述

Portless 解决本地开发中最古老的摩擦之一：端口号。把 `"dev": "next dev"`（http://localhost:3000）改成 `"dev": "portless run next dev"`，就得到稳定的 https://myapp.localhost——默认启用 HTTPS + HTTP/2，首次运行自动生成本地 CA 并信任、绑定 443 端口。

代理自动启动，通过 `PORT` 环境变量给应用分配随机端口（4000-4999）；对忽略 `PORT` 的框架（Vite、Astro、Expo 等）自动注入正确的 `--port`/`--host` 参数，且注入逻辑非常克制——复合命令、注释、env 前缀等无法安全注入的场景一律放行不动。项目副标题"For humans and agents"点明其双重受众：编码 Agent 同样受益于稳定、可预测的命名 URL。

---

## 核心功能

| 功能 | 说明 |
|------|------|
| 命名 URL | `https://myapp.localhost` 替代 `localhost:3xxx` |
| HTTPS + HTTP/2 默认开启 | 自动生成本地 CA 并加入信任链 |
| 框架自动适配 | 尊重 PORT 的框架直用；Vite/Astro/Expo 等自动注入端口参数 |
| Monorepo 支持 | 根目录一个 portless.json 覆盖所有 workspace 包，命名自动推断 |
| Turborepo 集成 | dev 脚本写 portless，turbo.json 零改动 |
| portless doctor | 一键体检：Node、代理、路由、CA 信任、DNS 解析 |
| hosts 自动同步 | Safari 等 DNS 场景自动写 /etc/hosts |
| 环形代理检测 | 前端代理未改 Host 头时返回 508 + 修复指引 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 运行时 | Node.js 24+ |
| 架构 | pnpm workspace monorepo + Turborepo |
| 状态 | `~/.portless`（sudo 下自动解析真实用户主目录） |
| 可选外联 | Tailscale（--tailscale/--funnel）、ngrok |

---

## 项目亮点

### 一个命令消灭"哪个端口是谁"
微服务/monorepo 场景下 `api.myapp.localhost` 比 `localhost:3000~3999` 的记忆负担低一个量级，Agent 也能稳定引用。

### 注入逻辑的工程克制
对无法安全注入的命令形态（管道、注释、env 前缀、委托脚本）明确拒绝并保持原样，宁可少做不做错——工具类软件少见的边界感。

### 本地 HTTPS 零配置
OAuth 回调、Service Worker、HTTP/2 测试等需要 HTTPS 的本地场景开箱即用。

---

## 应用场景

### Monorepo 多应用本地联调
前后端 + 多个子应用全部获得稳定命名域名，代理配置一次写对不再漂移。

### OAuth / Webhook 本地开发
HTTPS 域名回调地址可预先注册到第三方平台，端口号变化不再破坏回调。

### Agent 驱动的开发工作流
Agent 在终端输出稳定 URL 引用，人机协作时不再需要口头对端口号。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| ⭐ 总 Stars | 11,631 |
| 🍴 总 Forks | 381 |
| 📈 今日新增 | 69 |

---

## 📋 更新记录

### 更新 1 — 2026 年 9 月 3 日（再次登上 Trending）

**更新原因**：项目再次登上 GitHub Trending 榜单

**最新 Star 数据**：

| 总 Stars | 11,517 | 11,631 | +114 |
| 总 Forks | 378 | 381 | +3 |

**核心变化概要**：
- Star 数 11,517 → 11,631（+114），连续第 2 天登上 Trending
- Forks 378 → 381（+3），Vercel Labs 出品稳步积累口碑
- 突破 1.15 万 Star，本地开发环境零端口冲突方案持续受关注

---

## 总结

Vercel 出品的本地开发体验工具：一条命令把端口号换成带 HTTPS 的稳定命名域名，框架适配与 monorepo 支持细致入微——小切口、高完成度的开发者工效典型。

---

*数据来源：GitHub 仓库 (vercel-labs/portless)，2026 年 9 月访问*
*首次分析：2026 年 9 月 | 最近更新：2026 年 9 月 3 日*
