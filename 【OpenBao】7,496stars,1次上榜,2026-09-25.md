# OpenBao 项目分析

## 项目名称
**OpenBao** — 管理、存储与分发敏感数据（机密、证书、密钥）的开源软件
- **GitHub**: [openbao/openbao](https://github.com/openbao/openbao)
- **许可证**: MPL-2.0

---

## 项目概述

OpenBao 是 HashiCorp Vault 的开源社区分支（fork）。2023 年 HashiCorp 将 Vault 的许可证从 MPL 2.0 改为 BSL（商业源码许可证）后，社区在 Linux Foundation / OpenSSF 治理下发起 OpenBao，目标是「以 OSI 批准的开源许可证、开放治理原则」继续发展这款机密管理软件。它继承了 Vault 的完整能力：现代系统需要访问大量机密——数据库凭据、外部服务 API Key、SOA 通信凭据等，理解"谁在访问什么机密"本就困难，加上密钥轮换、安全存储与详细审计几乎必须依赖专门系统，OpenBao 正是为此而生。

项目治理规范完善（CHARTER.md、GOVERNANCE.md、MAINTAINERS.md），有多个专项工作组（namespaces、PKCS#11、scalability、supply chain、UI），OpenSSF Scorecard 与 Best Practices 徽章齐全，21,008 commits 显示从 Vault fork 后的开发持续活跃。

---

## 核心功能

| 功能 | 描述 |
|------|------|
| 安全机密存储 | 任意 KV 机密加密后写入持久存储（磁盘、PostgreSQL 等），拿到原始存储也拿不到机密 |
| 动态机密 | 按需为 AWS、SQL 数据库等系统生成凭据（如 S3 访问用的限定权限密钥对），租约到期自动回收 |
| 数据加密 | 不存储数据的加解密（encryption as a service），安全团队定义加密参数，开发者在 SQL 等存储中保存密文而无需自研加密 |
| 租约与续期 | 所有机密关联租约，到期自动回收，客户端可经内置 API 续期 |
| 吊销 | 支持吊销单个机密或整棵机密树（如某用户读取过的全部机密），服务于密钥轮换与入侵后封锁 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 语言 | Go（Go Modules） |
| 存储 | 磁盘、PostgreSQL 等可插拔后端 |
| UI | 内置 Web UI（ui/ 子树） |
| 可复用库 | github.com/openbao/openbao/api/v2 与 sdk/v2 供第三方导入 |
| 治理 | OpenSSF / Linux Foundation，Zulip 社区频道 |

---

## 项目亮点

### BSL 事件后的社区承接
OpenBao 是「厂商改许可证、社区分叉承接」模式在基础设施软件领域的标志性案例——开放治理章程 + 多公司维护者结构，避免单点厂商绑定，已被多个发行版（Debian、Fedora 等）收录为 Vault 替代。

### 安全工程成熟度
专职安全披露通道（openbao-security@lists.openssf.org）、OpenSSF Scorecard、Best Practices 徽章、供应链工作组——安全关键软件自身的安全流程做到了示范级。

### 与 Vault 生态兼容
API/SDK 双线发布（api/v2、sdk/v2），存量 Vault 用户可低成本迁移；namespaces、PKCS#11、可扩展性等方向有专项工作组持续推进。

---

## 应用场景

### 云原生机密管理
为 K8s 集群中的工作负载按需下发数据库凭据与服务间通信证书，配合动态机密实现短生命周期凭据。

### 合规驱动的密钥治理
审计日志 + 细粒度吊销 + 密钥轮换，满足金融/政企对敏感数据访问的合规要求。

### 摆脱厂商锁定的 Vault 替代
BSL 许可下无法自由使用 Vault 的场景（发行版打包、多云厂商内置），OpenBao 提供 MPL-2.0 的可信任替代。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| 总 Stars | 7,496 |
| 总 Forks | 580 |
| 今日新增 | +16 |
| 许可证 | MPL-2.0 |
| 主要语言 | Go |
| 提交数 | 21,008 |

---

## 总结

OpenBao 是 Vault 商业化改证后由 OpenSSF/LF 治理的开源承接者——完整的机密管理能力（动态凭据、加密服务、租约吊销）+ 开放治理 + MPL-2.0，是规避厂商锁定的机密管理基础设施首选之一。

---

*数据来源：GitHub 仓库 (openbao/openbao)，2026 年 9 月访问*
