# 项目名称

**AI Website Cloner Template**（AI 网站克隆模板）

GitHub 地址：https://github.com/JCodesMore/ai-website-cloner-template

---

## 项目概述

AI Website Cloner Template 是一个开源的可复用项目模板，能够利用 AI 编码代理（AI Coding Agent）将任意现有网站逆向工程为清洁、现代化的 Next.js 代码库。该项目由开发者 JCodesMore 于 2025 年 3 月发布，凭借"一条命令克隆网站"的核心理念迅速走红，发布后 3 天内即获得超过 6,000 颗 Star，截至目前已累计获得超过 17,500 颗 Star 和 2,700+ Fork，成为 GitHub 上最受关注的 AI 编码代理技能项目之一。

项目采用多阶段流水线架构：首先通过浏览器自动化工具（如 Chrome MCP）打开目标网站并截图，然后提取设计令牌（Design Tokens，包括字体、颜色、间距、动画等），接着为每个页面区块编写组件规格文件（Spec），最后调度多个并行构建代理各自在独立分支上重建网站的不同部分，最终合并结果并与原站进行视觉对比修正。整个过程从分析到生成完整的 Next.js 应用，只需用户输入 `/clone-website <URL>` 一条指令即可自动完成，耗时约 15 分钟至 1 小时（取决于目标网站的复杂度）。

该项目定位为 Claude Code 的"技能"（Skill），本质上是一个精心编写的 Markdown 指令文件（SKILL.md），它将通用的 LLM 转化为专门执行网站克隆任务的智能代理。项目通过 `sync-skills.mjs` 脚本将技能文件同步至所有 13 种支持的 AI 平台，确保无论用户使用哪种工具，都能获得一致的使用体验。项目采用 MIT 许可证，以 TypeScript 为主要开发语言。

---

## 核心功能

### 多阶段自动化流水线

项目定义了一套完整的网站克隆流水线，包含以下步骤：打开目标网站 → 全页截图 → 提取字体、调色板和全局样式 → 滚动页面并模拟交互（点击、悬停）→ 创建页面区块映射（头部、导航、主内容区、侧边栏、页脚等）→ 下载所有图片和图标 → 为每个区块编写规格文件并启动构建代理 → 合并结果 → 将克隆结果与原站进行对比并修复差异。这套流水线覆盖了从分析到交付的完整生命周期，确保克隆结果的高度还原。

### 多 AI 平台兼容

AI Website Cloner Template 不绑定于单一的 AI 编码工具，而是同时支持 13 种主流 AI 编码代理平台，包括 Claude Code、Codex CLI、OpenCode、GitHub Copilot、Cursor、Windsurf、Gemini CLI、Cline、Roo Code、Continue、Amazon Q、Augment Code 和 Aider。项目通过统一的 `AGENTS.md` 文件作为所有代理平台的单一事实来源（Single Source of Truth），并利用 `sync-skills.mjs` 脚本自动将技能文件同步至各平台对应的目录（如 `.claude/skills/`、`.codex/skills/` 等），极大降低了多平台适配的维护成本。

### 并行构建代理调度

在组件规格编写完成后，AI 代理会启动多个并行构建器（Builder Agent），每个构建器在独立的 Git 分支上负责重建网站的不同区块（如头部、主内容、页脚等）。这种"分而治之"的并行策略不仅提升了构建效率，也确保了各区块的隔离性和可维护性。最终所有分支的结果会被合并，并进行视觉对比校验。

### 设计令牌与组件规格自动提取

项目能够从目标网站中自动提取完整的 Design System，包括颜色变量、字体配置、间距系统、动画效果、交互状态（hover、active 等），并为每个页面区块生成精确的规格文件。这些规格文件记录了每个组件的确切数值参数，使 AI 代理在重建时能够做到像素级别的还原，而非模糊的"风格近似"。

---

## 技术栈

| 技术 | 用途 |
|------|------|
| **Next.js** | 应用框架，提供 SSR/SSG、路由及完整的 React 开发体验 |
| **React** | UI 组件库基础 |
| **TypeScript** | 主要开发语言，提供类型安全保障 |
| **shadcn/ui** | UI 组件库，提供现代化的基础组件 |
| **Tailwind CSS / PostCSS** | 样式系统，实现原子化 CSS |
| **ESLint** | 代码质量与风格检查 |
| **Docker** | 容器化部署支持 |
| **Chrome MCP** | 浏览器自动化，用于页面截图和交互模拟 |
| **Node.js** | 运行时环境与脚本工具链 |

---

## 项目亮点

### 一条命令的极致简捷性

用户只需执行 `/clone-website <URL>` 即可触发完整的克隆流程，无需手动分析网站结构、提取样式或编写代码。这种"零门槛"的设计哲学使得即使是非技术用户也能借助 AI 代理完成复杂的网站逆向工程任务。项目推荐使用 Claude Code 配合 Opus 4.7 模型以获得最佳效果，但在其他 12 种 AI 平台上同样可以运行。

### 规范化的 AI 技能架构

项目开创性地将"AI 技能"（Skill）概念应用于实际工程场景。每个技能以 Markdown 格式存储，包含详细的执行算法和步骤指令，本质上是一个将通用 LLM 转化为专业代理的结构化提示词。`AGENTS.md` 作为统一的项目指引文件，为所有 AI 平台提供一致的上下文和约束条件。这种架构不仅适用于网站克隆，也为 AI 辅助开发提供了可复用的模式。

### 完善的项目模板结构

项目提供了清晰的开箱即用模板结构，包括 `src/app`（页面路由）、`src/components`（业务组件）、`src/ui`（基础 UI 组件）、`public/images`（静态资源）和 `docs/research`（研究文档）等目录。用户通过 GitHub 的"Use this template"功能创建新仓库后，即可直接开始使用，无需额外配置。

### 道德与法律免责声明

项目作者 JCodesMore 明确声明该工具**不用于**钓鱼攻击、窃取他人设计或违反网站使用条款。Logo、品牌组件和受版权保护的内容归各自所有者所有。这种负责任的开源态度有助于项目的长期健康发展。

---

## 应用场景

### 平台迁移与重建

当企业或开发者需要将现有网站从 WordPress、Webflow、Squarespace 等传统建站平台迁移至现代技术栈（如 Next.js + React）时，AI Website Cloner 可以自动分析原始网站的设计系统并生成等效的现代化代码，大幅降低手动重建的工作量和成本。

### 源码丢失恢复

在代码仓库丢失、开发者离职或技术栈严重过时的场景下，该项目能够以现有网站为输入，快速生成一个具有完整视觉结构的起点项目。虽然生成结果可能无法做到 100% 业务逻辑还原，但在保留视觉设计和布局结构方面提供了极大的价值。

### 前端学习与分析

对于学习前端开发的开发者而言，该项目是一个强大的分析工具。它能将任意网站分解为具体的组件结构、设计令牌值、动画模式和交互状态，帮助学习者理解优秀网站是如何组织和实现的，而非依赖模糊的"查看源代码"方式。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| **总 Star 数** | 17,529 |
| **总 Fork 数** | 2,721 |
| **今日新增 Star** | 63 |
| **开源许可证** | MIT |
| **主要语言** | TypeScript |
| **创建时间** | 2025-03-20 |
| **GitHub 话题** | ai, cloner, claud-code, website, nextjs, template, reverse-engineering |

---

## 总结

AI Website Cloner Template 是 2025-2026 年度 AI 辅助开发领域最具代表性的开源项目之一。它巧妙地将 AI 编码代理（特别是 Claude Code 的技能系统）与网站逆向工程相结合，通过一条命令即可完成从网站分析、设计系统提取到代码生成的全流程自动化。项目支持 13 种主流 AI 平台，采用并行构建策略和精确的组件规格文件，在克隆质量与效率上达到了实用水平。

从更宏观的视角来看，该项目展示了 AI 辅助开发的新范式：不再是简单的代码补全或生成，而是将复杂的工程任务分解为结构化的流水线，让 AI 代理按既定算法协同执行。`AGENTS.md` 作为统一指令源、`sync-skills.mjs` 实现多平台同步的设计模式，为整个 AI 编码工具生态提供了有价值的参考架构。17,500+ Star 的社区热度也印证了市场对这类"AI 自动化工程"工具的强烈需求。对于希望探索 AI 代理实际工程应用、前端设计系统分析或网站快速重建的开发者而言，该项目无疑是值得深入研究和使用的重要工具。
