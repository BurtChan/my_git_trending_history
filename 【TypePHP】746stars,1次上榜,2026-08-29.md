# TypePHP 项目分析

## 项目名称
**TypePHP** — Swoole 团队开源的 PHP AOT 编译器，将 PHP 源码编译为原生机器码
- **GitHub**: [swoole/typephp](https://github.com/swoole/typephp)
- **许可证**: GPL-3.0

---

## 项目概述
TypePHP 是 Swoole（上海识沃网络科技）推出的 Ahead-Of-Time（提前编译）编译器，核心思路是把 PHP 源代码先翻译为 C++17，再编译为原生机器码。与字节码缓存（OPcache）或 JIT 虚拟机不同，它不在运行时解释 opcode，而是直接生成可脱离 PHP 运行时、跑在 CPU 上的原生二进制。项目 2026 年 5 月创建，8 月底提前开源（原计划 10 月发布），开源即冲上 GitHub Trending。

最引人注目的技术特征是**自举（self-hosting）**：整个编译器用纯 PHP 编写，`tpc` 编译器二进制是由它自己把自身的 PHP 源码编译出来的，引导链中没有任何 C/C++ 胶水代码。官方基准测试显示，在标准 PHP 语言基准上可获得约 6.5x–8x 加速，数组操作最高快 10 倍。

---

## 核心功能
| 功能 | 描述 |
|------|------|
| AOT 编译 | PHP → C++17 → 原生机器码，无解释器、无 opcode 缓存、无 JIT 预热 |
| 三种构建模式 | 同一代码库可产出原生可执行文件（bin）、PHP 扩展（ext）、共享库（lib） |
| 自举编译器 | 编译器全部用 PHP 实现，tpc 二进制由自身编译生成 |
| 原生数值类型 | bigInt、decimal、bigFloat 高精度数值，性能敏感计算无需扩展 |
| 强类型容器 | C++ 风格强类型泛型容器（Vector/Map 等），编译期类型检查 |
| 编译期属性 | `#[Immutable]` 只读契约、`#[ArrayDef]` 类型化数组契约、Property Hooks 降级 |
| 混合互操作 | 基于 C++ ABI 可直接调用 C/C++/Rust/Go 库；Zend 集成保证 Composer 包、内置函数与原生扩展继续可用 |
| 跨平台 | Linux x64/ARM64、macOS ARM64、Windows x64 完整发布包，另有 WASI 目标 |

---

## 技术栈
| 组件 | 技术 |
|------|------|
| 编译器实现 | 纯 PHP 8.4/8.5（自举） |
| 中间产物 | C++17 |
| 目标产物 | 原生机器码（bin/ext/lib）、WASM（WASI） |
| 分发 | Composer（`composer require --dev swoole/typephp`）、预编译二进制包 |
| 测试 | PHPUnit、PHPStan、覆盖率证据矩阵 |

---

## 项目亮点
### 真 AOT，不是 JIT
PHP 生态此前的性能方案（OPcache、JIT）本质仍依赖 ZendVM 运行时。TypePHP 把编译完全提前到构建期，二进制直接运行在 CPU 上，冷启动即为峰值性能——这对 CLI 工具、Serverless 冷启动场景是质变。

### PHP 子集 + Zend 兼容的双轨设计
它支持的是 PHP 8.4/8.5 的一个静态可分析的子集（强类型风格），但通过 Zend 集成保证动态生态兼容：Composer 包、内置函数、原生扩展都能继续工作，降低迁移门槛。

### 自举证明了语言能力
编译器这种复杂度的系统都能用 PHP 写并编译为原生二进制，本身就是对「PHP 只能做 Web」刻板印象的有力回击，也是对子集表达能力的最好验证。

### 桌面与嵌入式想象空间
由于产物是标准 C++ ABI 的原生库/可执行文件，理论上可以用 PHP 开发原生桌面软件、嵌入式组件，甚至通过 WASI 跑在浏览器边缘环境。

---

## 应用场景
### CPU 密集型 PHP 服务
数学计算、加密、图像处理等场景从 JIT 换到 AOT 可获得数倍加速，无需重写为 Go/Rust。

### CLI 工具分发
把 PHP CLI 工具编译为单二进制分发，用户无需安装 PHP 运行时，彻底解决 PHP 工具链分发难题。

### PHP 扩展开发
用 PHP 本身写 PHP 扩展（ext 模式），替代传统的 C/ZPP 开发流程，显著降低扩展开发门槛。

### 高性能库组件
以共享库（lib）形式被 C/C++/Rust/Go 项目直接链接，让 PHP 积累的算法代码复用到其他语言生态。

---

## Star 数据
| 指标 | 数值 |
|------|------|
| ⭐ 总 Stars | 746 |
| 🍴 总 Forks | 37 |
| 📈 今日新增 | 188 stars |
| 主要语言 | PHP |
| 创建时间 | 2026-05-11 |

---

## 总结
Swoole 用一个自举的纯 PHP AOT 编译器向「PHP 性能天花板」发起挑战——6.5x–8x 的实测加速、三模式产物和 Zend 生态兼容设计，让老牌 PHP 生态第一次拥有了通往原生性能的低成本路径；其 GPL-3.0 许可证和支持子集的限制（动态特性、eval 等不适用）是落地时需要权衡的代价。

---

*数据来源：GitHub 仓库 (swoole/typephp)，2026 年 8 月访问*
