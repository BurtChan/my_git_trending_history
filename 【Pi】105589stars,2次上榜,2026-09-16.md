# Pi 项目分析

## 项目名称
**Pi (Pi Agent Harness)** — AI Agent 工具箱：统一 LLM API + Agent 运行时 + TUI + 自我扩展的编码 Agent CLI
- **GitHub**: [earendil-works/pi](https://github.com/earendil-works/pi)
- **许可证**: MIT

---

## 项目概述

Pi 是 Mario Zechner（badlogic，libGDX 作者、前 Meta/Google 工程师）主导的 **AI Agent Harness 全家桶**，官网 pi.dev。它不是又一个编码 Agent，而是把 Agent 开发的各层拆成可独立使用又协同组合的 monorepo 工具箱：`pi-ai`（统一多提供商 LLM API）、`pi-agent-core`（带工具调用与状态管理的 Agent 运行时）、`pi-tui`（差分渲染终端 UI 库）、`pi-coding-agent`（交互式编码 Agent CLI，自我扩展是其招牌能力）、`chord`（服务组合运行时：复制状态、RPC、插件）与 `pi-telemetry`（厂商中立遥测契约）。

项目增长曲线惊人：105,386 Stars / 13,253 Forks、6,289 次提交，是本轮 AI 编码 Agent 浪潮中Stars 量级最高的项目之一。哲学上有几个鲜明立场：① **不内置权限系统**——默认以启动用户权限运行，需要边界就去容器化（文档给出 Gondolin 微 VM / Docker / OpenShell 三种沙箱模式）；② **供应链加固到偏执**——外部依赖全部钉死精确版本、`save-exact` + `min-release-age=2`（拒绝当日发布的依赖）、shrinkwrap 钉死传递依赖、发布前冒烟测试、生命周期脚本白名单；③ **开源协作新实验**——新贡献者的 issue/PR 默认自动关闭再由维护者每日审阅（对抗 Agent 生成的垃圾 PR），并公开号召用户用 `pi-share-hf` 把真实 OSS 编码会话发布到 HuggingFace（维护者带头发布自己的 pi-mono 会话数据集），用真实工作负载而非玩具 benchmark 改进编码 Agent。

---

## 核心功能

| 功能 | 描述 |
|------|------|
| 统一 LLM API | pi-ai：OpenAI/Anthropic/Google 等多提供商一套接口 |
| Agent 运行时 | pi-agent-core：工具调用、状态管理、Agent 循环 |
| 编码 Agent CLI | pi-coding-agent：交互式、可自我扩展（agent 自己给自己写扩展） |
| TUI 库 | pi-tui：差分渲染的终端 UI 组件 |
| 服务组合 | chord：复制状态、RPC、插件的应用组合运行时 |
| 会话分享 | pi-share-hf：把编码会话发布到 HuggingFace 供社区研究 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 语言 | TypeScript（Node/Bun 双运行时支持） |
| 包管理 | npm monorepo + 精确版本钉死 + shrinkwrap |
| 质量门禁 | biome、类型检查、LLM 无关测试跳过策略 |
| 沙箱方案 | Gondolin 微 VM / Docker / OpenShell 三模式 |
| 发布 | 版本化源码归档 + SHA256SUMS + 离线模型数据构建 |

---

## 项目亮点

### 「Harness 而非产品」的分层设计
LLM API、Agent 运行时、TUI、CLI 层层可拆用，既是终端用户产品也是开发者构建自有 Agent 的基础设施，天花板远高于单体编码工具。

### 对 Agent 时代开源治理的先锋实验
自动关闭新贡献者 PR 对抗 LLM 垃圾贡献 + 公开真实编码会话数据集，两个动作都在直面试图回答「AI 大量写代码之后开源怎么办」。

### 教科书级供应链安全
依赖钉死、发布冒烟、生命周期脚本白名单、npm audit 签名校验——对一个安装量巨大的 CLI 来说，这套纪律本身就是行业示范。

---

## 应用场景

### 日常编码主力 CLI
想用一个轻量、可自我扩展、不被厂商锁定的终端编码 Agent 的开发者。

### 构建自有 Agent 应用
直接复用 pi-ai / pi-agent-core / pi-tui 组装垂直场景 Agent，而不必从零写适配层。

### Agent 行为研究
利用公开的 pi-mono 会话数据集研究真实编码任务中的工具使用、失败与修复模式。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| 总 Stars | 105,589 |
| 总 Forks | 13,273 |
| 今日新增 | +203 |

---

## 📋 更新记录

### 更新 1 — 2026 年 9 月 16 日

**更新原因**：连续第二日在榜，超大基数下保持稳健增长。

**最新动态**：
- 总 Stars 105,386 → 105,589（+203），维持 105K+ 头部体量
- Forks 13,253 → 13,273（+20），贡献面稳定
- 连续两日在榜（9/15–9/16），Agent 信任基础设施关注度延续

**Star 数据**：

| 指标 | 上次记录 | 最新数据 | 变化 |
|---|---|---|---|
| 总 Stars | 105,386 | 105,589 | +203 |
| 总 Forks | 13,253 | 13,273 | +20 |
| 今日新增 | — | +203 | — |
**核心变化概要**：
- 总 Stars 105,386 → 105,589（+203），维持 105K+ 头部体量
- Forks 13,253 → 13,273（+20），贡献面稳定
- 连续两日在榜（9/15–9/16），Agent 信任基础设施关注度延续


## 总结

libGDX 作者打造的 AI Agent 全家桶：从统一 LLM API 到可自我扩展的编码 CLI 分层解耦，以 105K Stars 成为 Agent 基础设施赛道的头部项目，其对抗 Agent 垃圾 PR 与公开真实会话数据的治理实验同样具有行业标本价值。

---

*数据来源：GitHub 仓库 (earendil-works/pi)，2026 年 9 月访问*
*首次分析：2026 年 9 月 15 日 | 最近更新：2026 年 9 月 15 日*
