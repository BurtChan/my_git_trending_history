# StarNet 项目分析

## 项目名称
**StarNet** — 一个活的像素风空间站，真实 AI Agent 在其中做真实工作
- **GitHub**: [androoAGI/starnet](https://github.com/androoAGI/starnet)
- **许可证**: MIT（代码；StarNet 名称/Logo/美术资产保留权利）

---

## 项目概述

StarNet 是一个本地优先（local-first）的桌面 Agent Harness：你创建 AI Agent，把它们组织进一个像素风空间站，然后观看它们用真实模型和工具执行真实工作。空间站不是装饰——它是运行时状态的实时投影，产品契约字面成立：**一个房间 = 一个能力受限的团队**，**一条走廊 = 一条授权交接通道**，**一个摆放的物件 = 一次真实的能力授予**。你画的布局就是 Agent 运行的工作流。

项目由 Andrew Sims（androoAGI）开发，基于 Tauri（Rust 桌面壳）+ Node sidecar（本地 Agent 运行时）+ 原生 JavaScript 前端。核心产品法则：**界面绝不呈现 harness 无法证明的状态**——不模拟收入、不模拟完成、不模拟模型活动或开销。

---

## 核心功能

| 功能 | 描述 |
|------|------|
| Agent 船员而非聊天机器人 | 创建具有不同职业、人设与装备的 Agent，可同时运行多个——每个有独立工作区、转录、记忆与受限权限 |
| 自带模型密钥 | 粘贴 OpenRouter Key 或登录 Anthropic/OpenAI/Google 账号；密钥存 OS 钥匙串，绝不进前端；也支持 Ollama 本地模型零成本运行 |
| 全渠道消息 | 将 Agent 接入 Telegram/Discord/Slack/Signal/Matrix，离开桌面也能与空间站对话 |
| Night Shift | 留站运行时 Agent 在明确可调的"缰绳"内继续工作，每个离席动作都有日志可审查 |
| Recipes/Skills/定时任务 | 启动多步骤配方、授予可复用技能、以 cron 计划任务产出可见成果 |
| Task Briefs | 遇到歧义时主动通过任意渠道问一个具体问题，而不是静默猜错 |
| OUTBOX 交付 | 成果以真实文件落盘，而不是要考古的聊天记录 |
| MCP 连接器 | 挂载 Model Context Protocol 服务器与粘贴密钥/OAuth 连接器扩展 Agent 能力 |
| 语音 | 按住说话输入，统一空间站语音输出 |
| 真实账本 | 开销、预算、运行历史持久化在磁盘上并原样展示 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 桌面壳 | Rust / Tauri（Windows 10/11、macOS Intel & Apple Silicon） |
| Agent 运行时 | Node sidecar（providers、工具、持久化、预算、授权） |
| 前端 | 原生 JavaScript 站内世界 + 桌面 UI（localhost HTTP/NDJSON + SSE 消费 sidecar 事件） |
| 密钥存储 | OS 钥匙串（sidecar 本地权威持有） |
| 迁移 | 可从 OpenClaw / Hermes 的本地目录导入既有 Agent（人设/指令/记忆/模型） |

---

## 项目亮点

### 空间即权限：可视化能力模型
像素站的每个实体都对应真实的运行时语义——房间是能力作用域、走廊是授权交接、物件是能力授予。把"权限管理"这个抽象概念变成可绘制的空间布局，是 Agent 可视化编排的一种新范式。

### 反模拟的诚实契约
项目明确"不模拟任何东西"：真实模型调用、真实工具、真实成本。界面不得断言 harness 无法证明的状态，这在 Agent 产品普遍"表演式演示"的环境里是稀缺的工程品格。

### 本地优先 + BYO Key
密钥留在本机钥匙串、数据留在本机磁盘、可选 Ollama 完全离线运行——对隐私敏感与成本敏感用户都友好。

---

## 应用场景

### 个人 Agent 工作站
把多个专职 Agent（研究、编码、运维）部署在同一空间站，通过 Night Shift 让它们夜间持续处理任务队列，早上从 OUTBOX 收文件。

### 多渠道私人助理
Agent 通过 Telegram/Signal 等渠道随时响应，Task Briefs 机制保证它在不确定时先问再做。

### Agent Harness 实验
作为开源 harness，可研究 sidecar 事件流、权限授予模型、QA 门禁（`npm run qa:ready` 候选绑定验收）等工程实践。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| 总 Stars | 400 |
| 总 Forks | 82 |
| 今日新增 | +118 |
| 许可证 | MIT |
| 主要语言 | JavaScript |

项目 2026 年 9 月 23 日仍有提交，当前处于早期爆发阶段，单日 +113 的增速对小体量项目而言值得关注。

## 📋 更新记录

### 更新 1 — 2026年9月26日

上榜第 2 次。Star 从 276 增至 400（+124），Forks 增至 82（++9）。

**当日动态**：连续第二日上榜，Star 276 → 400（+45%），Forks 73 → 82。作为「像素艺术空间站 + 本地 Agent Harness」的桌面产品，靠 BYO Key、真实模型调用与成本账本的诚实性契约在 Agent 体验产品化方向持续获得关注；最近提交（9 月 23 日）仍在持续打磨桌面端体验。

**核心变化**：
- Star 276 → 400（+124，45%），增速在昨日接近翻倍后依然强劲；TODAY +118
- Forks 73 → 82（+9），社区开始出现衍生尝试
- 「空间布局即权限与工作流」的可视化 Agent 方向在独立开发者圈层持续发酵

| 指标 | 上次记录 | 最新数据 | 变化 |
|------|----------|----------|------|
| 总 Stars | 276 | 400 | +124 |
| 总 Forks | 73 | 82 | ++9 |
| 今日新增 | — | +118 | — |

---

*首次分析：2026 年 9 月 25 日 | 最近更新：2026 年 9 月 26 日*

---

## 总结

StarNet 用像素艺术空间站包装了一个严肃的本地 Agent Harness——空间布局即权限与工作流，真实模型调用与成本账本替代模拟表演，BYO Key + Ollama 本地优先。在"Agent 体验产品化"的探索中，它把可视化、权限模型和诚实性契约结合成了一个自洽的桌面产品。

---

*数据来源：GitHub 仓库 (androoAGI/starnet)，2026 年 9 月访问*
