# Treg 项目分析

## 项目名称
**Treg** — OpenRouter for agent tools：Agent 工具的统一路由网关
- **GitHub**: [superdesigndev/treg](https://github.com/superdesigndev/treg)
- **许可证**: Apache-2.0 附加条款（可自托管与商用，禁止转售为竞争托管服务）
- **官网**: [treg.to](https://treg.to)

---

## 项目概述

Treg 由 Superdesign 团队开发（2026 年 7 月创建，两个月即获近 2,000 stars），一句话定位是「OpenRouter，但路由的对象从模型换成 Agent 工具」。Agent 要完成真实工作往往卡在工具侧：Semrush $139/月、Moz $99/月、Crunchbase $99/月……这些数据能力都锁在没人会为单次运行购买的订阅墙后面。Treg 的解法是把 **60+ 供应商的 3,000+ 端点**（SEO 与外链、社媒与趋势、企业与人脉信息、广告、爬虫、图像视频生成）统一到一个 base URL + 一个 token 之后，**按调用计费、低至一美分、无需逐家注册**。

另一半能力面向团队内部：任何成员都可以把组织的付费 API key、OAuth 连接、厂商 CLI 或 `SKILL.md` 技能注册进网关，全队 Agent 共享调用，而**凭证永远不离开服务端**。代理层只做忠实中继、不做上游建模，凭据在服务端注入，因此上游 API 变化时调用方无感。

---

## 核心功能

| 功能 | 说明 |
|------|------|
| 工具目录（Catalog） | 3,000+ 端点、60+ 供应商，按调用计费（自有 key 用预付余额，匿名路由免费） |
| 自有工具注册 | 团队的 API key / OAuth / CLI / SKILL.md 技能包统一注册，全队共享且永不计费 |
| 服务端凭据注入 | 代理中继上游、绑定式注入多凭证（bearer + developer-token 等），调用方不持 key |
| OAuth 托管 | 手动上传 / 自动刷新 / hosted connect flow 三种接入方式 |
| 凭证健康检查 | 定期探测所有凭证有效性，失效自动 webhook 通知所有者 |
| 多端接入 | `treg` CLI、Claude Code 插件市场（`/plugin install treg@treg`）、Cursor 插件、npm 包 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 主语言 | Python |
| 部署 | Render 托管（treg.to）+ 支持自托管（deploy 目录、Docker） |
| CLI | treg 命令行（onboard / tool ls / health / run） |
| 代理架构 | 转发式 proxy + 注入器（injectors）+ 会话凭据剥离 |
| 测试 | pytest + xdist 全量覆盖（proxy/注入器/OAuth/健康检查/CLI） |

---

## 项目亮点

### 「按任务找工具，而不是按供应商」
开发者不需要知道哪家卖外链数据、也不需要持有它们的账号——`treg search "backlinks for a domain"` 按能力搜索、读价格、直接调用，把工具消费从订阅制变成 utility 计费。

### 凭证永不离开服务器
「中继而非建模」+ 服务端注入的设计让最敏感的团队 API key 不下发到任何客户端；自有工具调用永不计费、自有 key 优先于 Treg 的目录 key，企业安全边界清晰。

### 与 Agent 生态原生贴合
同时提供 Claude Code / Cursor 插件、`SKILL.md` 技能包注册与 MCP 路线图，目标是成为 Agent 工具层的默认网关——两个月 1,865 次提交的工程强度也表明这不是玩具项目。

---

## 应用场景

### Agent 一次接入全量数据工具
爬虫、SEO 分析、企业情报类 Agent 无需逐家采购订阅，通过单一 token 按次调用数十种数据能力。

### 团队凭证中枢
把组织的付费账号收敛进网关，成员的 Agent 凭 token 调用，凭据泄露面收敛到服务端一处。

### 技能市场内网化
将内部 CLI 与 SKILL.md 技能注册为团队资产，跨成员、跨 Agent 框架复用。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| ⭐ 总 Stars | 1,951 |
| 🍴 总 Forks | 206 |
| 📈 今日新增 | 197 stars |
| 🐛 未关闭 Issue | 80 |
| 📅 创建日期 | 2026-07-15 |
| 🔀 主语言 | Python |
| 📄 许可证 | Apache-2.0 附加条款 |

---

## 总结

Treg 把 OpenRouter 验证过的「聚合 + 按次计费 + 统一接入」模式搬到 Agent 工具层，一手打通 3,000+ 付费数据端点的长尾供给，一手用服务端凭证注入解决团队工具共享的安全痛点；在 Agent 工具生态（MCP、Skills）加速标准化的当下，它卡住的是「工具层统一网关」这个潜力位置——两个月近 2,000 stars 与 1,800+ 提交的执行力是其最直接的信用背书。

---

*数据来源：GitHub 仓库 (superdesigndev/treg)，2026 年 9 月访问*
