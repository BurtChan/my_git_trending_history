# CUPP 项目分析

## 项目名称
**CUPP** — Common User Passwords Profiler（通用用户密码分析器）
- **GitHub**: [Mebus/cupp](https://github.com/Mebus/cupp)
- **许可证**: GPL-3.0

---

## 项目概述

CUPP（Common User Passwords Profiler）是一款经典的开源密码字典生成工具，由安全研究员 Muris Kurgas（网名 j0rgan）于 2012 年创建，是网络安全领域中历史最悠久且持续被广泛使用的密码分析工具之一。项目以 Python 编写，采用 GPL-3.0 开源许可证，在 GitHub 上累计获得了超过 6,100 颗 Star 和 2,000 次 Fork。尽管创建于十余年前，CUPP 至今仍是渗透测试和安全研究人员的必备工具之一，2026 年仍在 GitHub Trending 榜单上获得大量关注，充分证明了其在安全社区中不可替代的经典地位。

CUPP 的核心设计理念源于一个朴素但极为有效的安全洞察：绝大多数用户在设置密码时会将个人信息融入其中——生日、姓名、昵称、宠物名、家人姓名、喜欢的球队、手机号码等。这些基于个人信息的密码虽然对用户自身而言容易记忆，但对于攻击者来说却是可预测的弱点。CUPP 通过交互式问答的方式收集目标用户的个人信息，然后运用多种组合策略（大小写变换、数字追加、特殊字符替换、常见模式拼接等）生成高度个性化的密码字典，显著提升字典攻击的成功率。

与通用的密码字典（如 rockyou.txt）相比，CUPP 生成的字典针对性更强、体积更小、效率更高。通用字典往往包含数百万条目，在穷举式攻击中消耗大量时间和计算资源；而 CUPP 基于目标画像生成的字典通常仅包含数千到数万条目，却能精准命中用户基于个人信息构造的弱密码。这种"以情报驱动攻击"的思路，正是 CUPP 在十余年间始终保持实用价值的核心原因。

---

## 核心功能

### 交互式用户密码分析（-i 模式）
CUPP 的核心功能是通过交互式问答收集目标用户的个人信息，生成定制化密码字典。问答涵盖多个维度：姓名（名、姓、昵称）、出生日期、伴侣/父母/兄弟姐妹/宠物姓名、手机号码、住址信息、公司名称、喜欢的运动队/音乐/电影、个人关键词等。收集完成后，CUPP 内置的生成引擎会基于这些信息进行多层组合变换——包括大小写全排列、数字后缀追加（123、1234、!@#）、字符替换（a→@、e→3、o→0）、年份拼接、生日格式变换（19850101、01/01/1985、850101）等——产生覆盖目标用户密码习惯的个性化字典。

### 已有字典分析（-w 模式）
CUPP 能够对已有的密码字典文件进行分析，提取其中的模式特征和统计信息。该模式帮助安全研究人员了解现有字典的构成特征，评估其对特定目标群体的覆盖效果，也可用于发现字典中可能遗漏的常见模式，从而指导字典的优化和补充。

### 大型词表下载（-l 模式）
CUPP 提供从网络下载大型密码词表的功能，为用户提供开箱即用的通用字典资源。该模式整合了多个公开可用的密码词表来源，方便用户快速获取基础字典，并可在此基础上进一步使用交互式模式进行个性化扩展。

### Alecto DB 解析（-a 模式）
CUPP 集成了对 Alecto 数据库的解析能力，能够从中提取设备、软件的默认用户名和密码组合。这些默认凭据是物联网设备、路由器、摄像头等嵌入式设备最常见的攻击入口之一，该模式为渗透测试人员提供了针对默认凭据的快速检测能力。

### 配置文件支持（cupp.cfg）
项目提供 YAML 格式的配置文件 cupp.cfg，允许用户预设个人信息参数，实现非交互式的批量字典生成。这对于需要针对多个目标同时生成字典的场景（如企业安全评估）尤为实用，也可以将配置文件纳入自动化工作流。

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 核心语言 | Python 3 |
| 运行环境 | 跨平台（Linux、macOS、Windows） |
| 密码生成引擎 | 内置多层组合变换算法 |
| 外部数据源 | Alecto DB（默认凭据数据库）、网络词表 |
| 配置管理 | cupp.cfg（YAML 格式） |
| 许可证 | GPL-3.0（完全开源） |
| 安装方式 | pip install 或直接克隆运行 |

---

## 项目亮点

### 十余年经典工具的持久生命力
CUPP 创建于 2012 年，至今已有超过 14 年的历史，是密码安全领域少有的"长青"开源工具。在安全工具快速更迭的今天，CUPP 依然被大量安全从业者使用和推荐，并在 2026 年 GitHub Trending 榜单上获得单日 32 颗 Star 的增长。这种持久生命力的根源在于其解决的是一个永恒的安全问题——人类密码行为的可预测性——而这一问题在 AI 时代依然存在，甚至因用户密码疲劳加剧而更加突出。

### 情报驱动的精准字典生成理念
CUPP 区别于其他密码工具的最大特色在于其"以人为中心"的字典生成思路。不是盲目扩大字典规模，而是通过社会工程学式的信息收集，构建目标的"密码画像"，再基于画像生成高度针对性的字典。这种思路与真实世界中绝大多数成功的高级持久威胁（APT）攻击中使用的方法一致——攻击者首先通过 OSINT 收集目标个人信息，再用工具生成针对性字典，最终突破认证防线。CUPP 将这一复杂流程简化为一个命令行工具即可完成的操作。

### 极简设计，零依赖运行
CUPP 的代码库极为精简，核心逻辑清晰易懂，没有复杂的外部依赖，一条 Python 脚本即可运行。这种极简设计不仅降低了使用门槛，也使其成为学习密码安全和字典攻击原理的优秀教学材料。对于安全培训和教育场景，CUPP 是介绍密码弱点和字典攻击概念的完美工具。

### 四种模式覆盖多种需求
从交互式个性化生成到字典分析、从大型词表下载到默认凭据解析，CUPP 的四种运行模式覆盖了密码字典相关的绝大多数需求场景。用户既可以使用 CUPP 从零构建字典，也可以在现有字典基础上进行分析和补充，灵活适应不同的渗透测试需求。

---

## 应用场景

### 渗透测试中的密码审计
在合法授权的渗透测试项目中，CUPP 是密码攻击阶段的利器。渗透测试人员首先通过社会工程学或 OSINT 收集目标组织员工的个人信息（LinkedIn 个人资料、社交媒体帖子、公开简历等），然后使用 CUPP 的交互式模式基于这些信息生成个性化密码字典，配合 Hydra、Hashcat、John the Ripper 等工具对目标系统的认证机制进行测试。相比于使用通用字典，CUPP 生成的字典通常能以更短的时间和更少的尝试次数发现弱密码。

### 安全意识培训与教育
CUPP 是展示密码弱点原理的绝佳教学工具。在企业安全意识培训中，安全团队可以现场演示 CUPP 的交互式问答过程，让员工直观看到自己的个人信息如何被转化为密码猜测——生日"1990-05-20"变成"May1990!"，宠物名"Max"变成"M@x2024#"等。这种直观的演示比任何安全策略文档都更能说服员工使用强密码和密码管理器。

### 取证调查与应急响应
在数字取证和安全事件响应场景中，CUPP 可以帮助分析人员快速生成针对特定嫌疑人的密码字典，用于解锁加密设备、解密保护文件或恢复密码保护的数据。配合已有证据（如社交媒体记录、通讯录信息），CUPP 能够显著提高取证解锁的成功率。

### 企业密码策略评估
企业安全团队可以使用 CUPP 的字典分析功能（-w 模式）和交互式生成功能，评估当前组织的密码策略是否足够抵御基于个人信息的字典攻击。通过模拟攻击，安全团队可以量化当前密码策略的实际安全效果，为密码复杂度要求的调整提供数据支撑。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| 总 Star | 6,149 |
| 今日新增 | 32 |
| Fork | 2,051 |
| 主要语言 | Python |
| 许可证 | GPL-3.0 |
| 创建时间 | 2012-05-06 |
| 主题标签 | dictionary-attack, password, password-strength, weak-passwords, wordlist, wordlist-generator |

---

## 总结

CUPP 是密码安全领域最具标志性的经典开源工具之一。自 2012 年由安全研究员 Muris Kurgas 创建以来，它凭借"以情报驱动字典生成"的独特理念和极简实用的设计，成为渗透测试人员的标准配置工具。在超过 14 年的历程中，CUPP 持续获得安全社区的认可——6,100+ Star、2,000+ Fork 的数据证明了其广泛的用户基础，而 2026 年仍在 GitHub Trending 上活跃则彰显了其跨越时代的实用价值。CUPP 提醒我们一个至今仍然成立的安全真理：人类行为的可预测性是密码安全最根本的弱点，而理解这一弱点、针对性地进行防御或测试，始终是信息安全工作的核心议题之一。

---

*数据来源：GitHub 仓库 (Mebus/cupp)，2026 年 7 月访问*
