# WechatOnCloud 项目分析

## 项目名称

**WechatOnCloud** — 云微信多开服务，在 NAS/服务器上运行多个独立微信实例

- **GitHub**: [Gloridust/WechatOnCloud](https://github.com/Gloridust/WechatOnCloud)
- **许可证**: 待确认

---

## 项目概述

WechatOnCloud（云微 WOC）是一个基于 Docker 容器技术的微信云多开解决方案，允许用户在自己的 NAS 或服务器上运行多个独立的服务端微信实例。每个微信实例运行在独立的 Docker 容器中，使用 Xvfb 虚拟显示和 KasmVNC 将微信画面串流到浏览器，实现跨设备消息同步和多端共享。

项目的核心理念是"自由连接"——用户可以通过网页浏览器远程操作微信，不修改微信客户端本体，使用官方原版 Linux 微信环境，理论上不存在封号风险。这是由开发者 @gloridust 受 @manateelazycat（懒猫微服）启发后独立开发的 ToB 开源版本，面向暂时买不起商用懒猫微服的用户。

系统架构非常清晰：宿主只需对外暴露面板的 36080 端口，所有微信实例容器仅在 Docker 网络内运行，由面板反向代理访问。微信本体不打包进镜像，而是在创建实例后由用户通过面板手动下载安装，因此镜像体积小、构建快速、不依赖腾讯 CDN。

---

## 核心功能

### 微信实例管理
每个微信实例 = 独立容器 + 独立数据卷 + 独立微信会话。支持创建、启动、停止、销毁实例，所有管理操作通过 Web 面板完成。

### 多用户共享
同一实例可被多个浏览器同时连接，共享同一个微信会话。支持子账号管理和权限控制，可分配不同微信实例给不同用户。

### 远程桌面操作
通过 KasmVNC 将每个微信实例的虚拟显示画面串流到浏览器，用户可以在网页上直接操作微信，包括音视频通话和文件传输。

### 一键部署
提供 docker-compose.yml，只需一条命令 `docker compose up -d` 即可拉起面板服务。支持本地构建和从 GHCR 拉取官方镜像两种部署方式。

### 多架构支持
支持 x86_64 和 arm64 架构，可部署在飞牛 NAS 等多种 NAS 设备上。

---

## 技术栈

| 组件 | 技术 |
|------|------|
| **容器化** | Docker + docker-compose |
| **虚拟显示** | Xvfb |
| **远程桌面** | KasmVNC |
| **面板** | 自研 Web 面板（TypeScript） |
| **容器编排** | Docker Socket（docker.sock 动态创建/销毁实例） |
| **镜像分发** | GHCR（GitHub Container Registry） |
| **认证** | Cookie 鉴权 + Basic Auth |

---

## 项目亮点

### 完全合规的云微信方案
使用官方原版 Linux 微信客户端，不修改微信本体，不注入任何代码。这种"远程桌面 + 官方客户端"的方式在合规性上远优于微信 Hook 类方案。

### 精巧的容器架构
面板容器通过 docker.sock 获得 Docker 引擎控制权，按需动态创建和销毁微信实例容器。宿主只需暴露一个端口，所有实例在 Docker 内部网络中运行，安全性极高。

### 轻量级镜像设计
微信本体不打包进镜像，按需下载到实例数据卷。这意味着镜像很小（仅面板程序和依赖），构建速度快，且不受腾讯 CDN 可用性影响。

### NAS 友好
专门适配飞牛 NAS（x86_64 / arm64），开箱即用。对于需要在家庭或小团队环境中共享微信会话的场景非常实用。

---

## 应用场景

### 家庭/团队微信共享
在 NAS 上运行多个微信实例，家庭成员或团队成员通过浏览器各自登录操作自己的微信会话，实现消息在不同设备间同步。

### 远程办公
在外出时通过浏览器远程访问家中 NAS 上的微信，处理工作消息，无需在手机和电脑间频繁切换。

### 微信客服/运营
运营团队可以同时管理多个微信客服账号，每个账号在独立容器中运行，互不干扰，且支持多人协作查看同一会话。

### NAS 玩家工具
作为 NAS 上的实用工具，WechatOnCloud 展示了 NAS 不只是存储设备，还可以是个人云服务平台，吸引更多用户探索 NAS 的高级玩法。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| **总 Stars** | ~1,879 |
| **总 Forks** | ~526 |
| **许可证** | 待确认 |
| **创建时间** | 2026 年 5 月 29 日 |
| **主要语言** | TypeScript |

---

## 总结

WechatOnCloud 是一个极具创意的 NAS 微信云多开解决方案，利用 Docker 容器 + 虚拟显示 + VNC 串流的技术组合，在服务器上运行官方原版微信客户端，通过浏览器远程操作。项目设计精巧、架构清晰、部署简单，是"懒猫微服"的开源平替方案，适合需要合规微信多开的家庭用户和小团队。

---

*数据来源：GitHub 仓库 (Gloridust/WechatOnCloud)，2026 年 6 月访问*
