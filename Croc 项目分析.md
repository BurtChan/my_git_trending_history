# Croc 项目分析

## 项目名称
**Croc** — 任何两台电脑间安全传文件的 CLI 工具
- **GitHub**: [schollz/croc](https://github.com/schollz/croc)
- **许可证**: MIT

---

## 项目概述

Croc 是一个用 Go 语言编写的命令行文件传输工具，能够让任何两台计算机之间简单、安全地传输文件和文件夹。与 scp、rsync 等传统文件传输工具不同，Croc 的核心理念是「零配置、跨平台、端到端加密」——发送方和接收方不需要在同一网络、不需要配置端口转发、不需要预先建立信任关系，只需一个简单的命令即可完成传输。

Croc 的工作原理是通过一个公共中继服务器建立连接，然后使用 PAKE（Password-Authenticated Key Exchange）协议在两端协商出共享加密密钥，所有数据传输均经过端到端加密，即使是中继服务器也无法读取传输内容。项目自 2017 年创建以来已积累超过 36,000 个 Star，成为 Go 语言生态中最受欢迎的文件传输工具之一，拥有 2,200+ 次提交的活跃开发历史。

Croc 的独特之处在于它同时解决了文件传输中的多个痛点：跨平台兼容性（Windows、Linux、macOS）、NAT 穿透（无需端口转发）、传输安全（PAKE 加密）、断点续传、多文件传输、IPv6/IPv4 双栈支持，以及 Tor 代理兼容性。这些特性使其成为开发者日常工作中不可或缺的实用工具。

---

## 核心功能

| 功能 | 描述 |
|------|------|
| **任意两台电脑传输** | 通过中继服务器连接，无需同一网络或端口转发 |
| **端到端加密** | 基于 PAKE 协议，即使中继服务器也无法读取数据 |
| **跨平台** | 原生支持 Windows、Linux、macOS，无需额外依赖 |
| **多文件传输** | 支持同时传输多个文件和整个目录 |
| **断点续传** | 中断的传输可以从中断点继续，无需重新开始 |
| **IPv6 优先** | 默认使用 IPv6，自动回退到 IPv4 |
| **代理支持** | 支持 Tor 等 SOCKS5 代理进行传输 |
| **零配置** | 无需安装本地服务器、无需配置端口转发 |
| **管道支持** | 支持通过 stdin/stdout 管道传输数据 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| **编程语言** | Go |
| **加密协议** | PAKE（Password-Authenticated Key Exchange） |
| **网络协议** | TCP + WebSocket |
| **中继服务器** | 内置公共中继 + 支持自建 |
| **构建工具** | GoReleaser（跨平台二进制发布） |
| **CI/CD** | GitHub Actions |

---

## 项目亮点

### 无与伦比的易用性
Croc 的使用体验极其简洁——发送方执行 `croc send file.txt`，终端会显示一个加密短语（如 `passphrase-code`），接收方只需执行 `croc passphrase-code` 即可接收文件。整个过程中用户无需关心 IP 地址、端口、防火墙等网络细节，Croc 自动处理所有连接建立和 NAT 穿透工作。这种「一个命令即可传输」的体验是传统工具（如 scp 需要指定用户名、主机名、路径）无法比拟的。

### 基于 PAKE 的端到端加密
Croc 采用 PAKE 协议实现密钥协商，这是一种专为「在不安全信道上安全建立共享密钥」设计的密码学协议。与传统 TLS 不同，PAKE 不依赖证书颁发机构，而是通过用户输入的短密码（Croc 自动生成的加密短语）来协商加密密钥。这意味着即使中继服务器被攻破，攻击者也无法解密传输数据，因为加密密钥从未经过网络传输。

### 高度活跃的长期维护
项目自 2017 年 10 月创建至今已有近 9 年历史，累计 2,242 次提交，展现出极高的开发活跃度和长期维护承诺。在开源工具领域，能持续维护近 10 年的项目屈指可数，这表明 Croc 已形成稳定的社区和可靠的质量保障。

### 自建中继与隐私控制
对于对数据隐私有更高要求的用户，Croc 支持自建中继服务器。用户可以在自己的服务器上部署 `croc relay`，所有传输数据仅经过自己的基础设施，完全不依赖第三方公共中继。同时，Croc 的 Tor 代理支持使得用户可以通过洋葱网络进行匿名文件传输，进一步增强了隐私保护能力。

---

## 应用场景

### 跨网络文件共享
当两台电脑不在同一局域网时（如办公电脑与家庭电脑、不同地区的同事之间），Croc 提供了最简单的文件传输方案。无需配置 VPN、无需端口转发、无需云存储中转，一条命令即可完成传输，特别适合临时性的跨网络文件交换需求。

### 自动化运维中的文件分发
在 DevOps 和系统运维场景中，Croc 可通过管道和脚本集成到自动化工作流中。运维人员可以将配置文件、日志文件、部署包等通过 `croc` 命令快速分发到多台服务器，配合断点续传功能确保大文件传输的可靠性。

### 隐私敏感数据传输
对于包含敏感信息（如密钥文件、数据库备份、个人文档）的文件传输，Croc 的 PAKE 端到端加密提供了强大的安全保障。结合自建中继和 Tor 代理支持，用户可以实现完全自主可控的隐私传输通道。

### 开发者日常工作流
作为 CLI 工具，Croc 完美融入开发者的终端工作流。在代码审查中分享测试数据、在不同开发环境间同步项目文件、与远程同事共享二进制构建产物——Croc 用最少的操作步骤完成这些日常任务，避免了打开浏览器登录云存储的繁琐流程。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| **总 Stars** | 36,466 |
| **总 Forks** | 1,442 |
| **今日新增 Stars** | ~511 |
| **主要语言** | Go |
| **创建时间** | 2017 年 10 月 |
| **总提交数** | 2,242 |
| **许可证** | MIT |

---

## 总结

Croc 是文件传输工具领域的标杆级开源项目，它以「零配置、端到端加密、跨平台」的极简理念，重新定义了命令行文件传输的体验标准。近 9 年的持续维护、36,000+ 的 Star 积累和 2,200+ 次提交的活跃开发，证明了其在开发者社区中不可替代的地位。对于任何需要在终端环境中传输文件的用户而言，Croc 都是值得安装和信赖的工具。

---

*数据来源：GitHub 仓库 (schollz/croc)，2026 年 7 月访问*