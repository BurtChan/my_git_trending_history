# wifit3 项目分析

## 项目名称
**wifit3** — Wifite 的跨平台继任者：一个仅依赖 USB 网卡、可在 Linux / Windows / macOS 三平台运行的独立 Wi-Fi 安全审计工具
- **GitHub**: [derv82/wifit3](https://github.com/derv82/wifit3)
- **许可证**: GPL-2.0（代码；固件 blob 按各厂商许可分发）

---

## 项目概述

wifit3 是知名 Wi-Fi 审计工具 Wifite 作者 derv82 的新作，定位是「Wifite but USB-only & cross-platform」。传统的 Wi-Fi 渗透测试几乎被 Linux 垄断——monitor mode（监听模式）和 frame injection（帧注入）依赖内核无线驱动的支持，Windows 的 NDIS 限制和 macOS 的封闭驱动体系让其他平台上的无线审计几乎不可能。wifit3 的突破点在于**完全绕过操作系统的原生 Wi-Fi 协议栈**：它内置了一组用 Python 移植的 Linux 内核无线驱动（mini-drivers），通过 USB bulk/control transfer 在用户态直接操作无线网卡的寄存器，实现用户态的监听模式与帧注入。这意味着同一套审计能力（扫描、WPA/WPA2 握手捕获、WPS Pixie-Dust 攻击等）可以在三大操作系统上以一致的方式运行。

项目 2026 年 6 月创建，迭代非常活跃（v0.3.1 / v0.3.2 / v0.3.3 三个 BETA 版本在 9 月 12/15/22 日密集发布），目前仍处于 BETA 阶段。作为 Wifite 的精神续作，它继承了「自动化无线审计」的产品哲学——用户只需插入一张受支持的 USB 网卡，点 START 即可完成驱动配置（Linux 下自动写 udev/modprobe 规则、Windows 下自动绑定 WinUSB），无需手动折腾 airmon-ng。

值得注意的是项目对 Linux 社区的致意：README 明确感谢逆向工程并维护这些 Linux 无线驱动的开发者——wifit3 的 mini-drivers 正是从这些内核驱动移植而来。

---

## 核心功能

| 功能 | 说明 |
|------|------|
| 跨平台运行 | Linux / Windows / macOS 三平台行为一致，突破 Windows NDIS 限制 |
| 用户态 mini-drivers | Python 移植的 Linux 内核驱动，直接 USB 寄存器级控制，无需内核驱动支持 |
| 侦察与分析 | 扫描周边 AP，聚焦单一目标的 Focus 模式，实时显示握手包状态 |
| 攻击与捕获 | WPA/WPA2/WPA3 握手捕获（输出 .pcap / .hc22000）、WPS Pixie-Dust、PushButton PSK 捕获 |
| 自动驱动配置 | START 按钮一键完成：Linux 写 udev + modprobe 规则，Windows 绑定 WinUSB 并触发 PnP 重扫 |
| 干净卸载 | 内置 Uninstall 流程，恢复网卡给操作系统原生 Wi-Fi 栈 |
| 预编译二进制 | Releases 提供 Windows x64 / Linux x64 独立可执行文件，零依赖开箱即用 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 语言 | Python（uv 管理依赖） |
| 架构 | 用户态 USB 驱动（bulk/control transfer 直控寄存器） |
| 构建打包 | PyInstaller（wifit3.spec）、Astral uv |
| 支持芯片 | Atheros AR9271、MediaTek MT7610U/7612U/7921AU/7925U、Realtek RTL8812AU/8814AU/8821AU/8821CU/8922AU/8822BU/8822CU/8187L/8188EUS、Ralink RT2570/3070/5370/5372/5572 等 19 种（对应 ALFA / TP-Link / Panda / Netgear / ASUS / D-Link 等常见网卡） |
| 工程配套 | 完整 tests、docs（含驱动移植方法论文档与 USB trace replay 工具）、CI |

---

## 项目亮点

### 用户态重写内核驱动，打破平台垄断
这是 wifit3 最硬核的部分：把 Linux 内核无线驱动移植成纯 Python 的用户态 mini-driver，通过 USB 传输直接读写网卡寄存器。帧注入与监听模式全部在用户态完成，绕开了 Windows NDIS 限制与 Linux 内核驱动锁定的双重枷锁，还附带完整的移植方法论文档和 USB trace 回放工具。

### 一键化的硬件配置体验
传统 Wi-Fi 审计最劝退的环节是驱动配置（airmon-ng、内核头文件、驱动编译）。wifit3 在应用内点击 START 即自动完成：Linux 下经 pkexec/sudo 写入 /etc/modprobe.d/ 与 udev 规则，Windows 下卸载原厂驱动改绑 WinUSB；退出时 Uninstall 一键还原。

### Wifite 作者的正统续作
derv82 是 Wifite（Kali Linux 内置工具）的原作者，对无线审计工作流的理解深厚。wifit3 继承了 Wifite 的自动化交互风格（扫描 → 选目标 → 自动攻击），但架构上彻底换代，9 月内连发三个 BETA，开发节奏很快。

### 硬件兼容清单详尽
README 维护了 19 种芯片方案、30+ 张具体网卡型号的兼容矩阵（含频段支持），并附每张网卡能力局限的专门文档——对需要选购硬件的渗透测试者非常实用。

---

## 应用场景

### 渗透测试者的统一工具箱
安全顾问日常可能在 Windows 笔记本上办公、在 Linux 虚拟机里做测试。wifit3 让一张 USB 网卡 + 一个可执行文件在任何平台完成无线审计，省去双系统或专用审计机的麻烦。

### 企业无线安全自查
IT 管理员可用它验证自家 WPA2/WPA3 部署的握手流程是否暴露于 Pixie-Dust 等攻击面，输出的 .hc22000 文件可直接交给 hashcat 做密码强度验证。

### 无线驱动研究学习
项目公开的驱动移植方法论（docs/porting/METHODOLOGY.md）与 USB trace replay 工具本身就是学习「如何把内核驱动搬到用户态」的稀缺教材，对做 USB 设备逆向、用户态驱动开发的人有参考价值。

### 硬件选购指南
准备入门无线安全的人可以把它 19 种芯片的兼容表当购物清单用——ALFA AWUS036 系列、TP-Link Archer T 系列、Panda PAU 系列都有明确支持状态。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| 总 Stars | 700 |
| 总 Forks | 76 |
| 今日新增 | +168 |
| Open Issues | 5 |
| 语言 | Python |
| 创建时间 | 2026-06-20 |
| 最新版本 | v0.3.3 (BETA)，2026-09-22 发布 |

---

## 总结

Wifite 作者把 Linux 内核无线驱动移植成 Python 用户态 mini-driver，让 Wi-Fi 审计第一次真正跨平台跑在 Linux/Windows/macOS 上——用一张 USB 网卡替代整套 Linux 审计环境，是无线安全工具今年最值得关注的架构创新之一。

---

*数据来源：GitHub 仓库 (derv82/wifit3)，2026 年 9 月访问*
