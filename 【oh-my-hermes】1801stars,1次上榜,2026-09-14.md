# oh-my-hermes 项目分析

## 项目名称
**oh-my-hermes** — 为 Hermes Agent 打造的一体化强化插件：编码智能、长期记忆与模型优化工作流
- **GitHub**: [rlaope/oh-my-hermes](https://github.com/rlaope/oh-my-hermes)
- **许可证**: MIT

---

## 项目概述

oh-my-hermes（OMH）是建立在 [NousResearch/hermes-agent](https://github.com/NousResearch/hermes-agent) 之上的操作层插件，理念是"Install once. Keep Hermes. Add a stronger operating layer."——它不替换 Hermes，也不在背后藏一个编码执行器，而是把一次普通的自然语言请求转化为明确的能力（capability）、有用的下一步（next step）和对实际发生事情的诚实记录（evidence boundaries）。OMH 位于 Hermes 原生 skills 之上负责框定问题、选择工作流与证据门控，再以受治理的路径调度原生技能执行。

项目定位非常克制：强化而非取代。它把"规划、研究、创作、编码交接、运维和项目记忆"这些日常操作封装成有明确证据边界的流程，针对 13 个模型家族逐一校准提示词并附实测数据，在安全的前提下并行拆分任务，最终以类型化结果回收。README 以终端横幅、能力海报和逐项 showcase 图详细展示了路由打分、类别链、校准块、并行工作树与长期记忆的真实运行面。

项目 2026 年 6 月初创建，三个多月即收获 1,800+ Stars，当前单日 +52 的增势显示其在 Hermes Agent 生态中的关注度正在稳步积累。

---

## 核心功能

| 功能 | 描述 |
|------|------|
| 请求路由打分 | 每个请求先打分再派发，改名类轻任务走 quick 通道，"找出 X 的所有引用"触发穷举信号走更强模型，所有影响分数的信号都具名可查 |
| 九类执行器类别链 | ultrabrain/deep/architect/quick/writing 等九条链各自是可编辑的"模型+力度"组合，单文件即可读取与覆盖，provider 拒绝时自动前进、不可服务时拒绝派发而非静默降级 |
| 逐模型家族校准 | 13 个模型家族各一个校准块，每句话针对该家族的已知特性撰写（如 Claude 告知清单已完整、Gemini 无工具输出的主张不算证据、Qwen3-Coder 不输出思考标签），并用实测数据修订 |
| 安全并行工作树 | ulw-work 把已接受的计划拆成互不共享文件的单元，各自从同一 pinned SHA 建工作树，单元工具调用单轮发出；结果四态类型化（进程退出/模式合法/验证可观测/集成就绪），exit 0 无证据只能停留在 reported done |
| 长期记忆系统 | 带评审记忆的设计，跨会话沉淀项目上下文，"a reviewer admitted" 的记忆质量自证 |
| Ultra-Skills 工作流包 | 针对编码/研究/创作等场景的优化工作流包，专家技能按需渗入运行过程 |
| Evidence Before Claims | 先证据后结论的治理原则贯穿全部输出，验证回执仅在修订版本、命令与环境三者匹配时才复用 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 主语言 | Python |
| 宿主 | Hermes Agent（NousResearch）插件体系 |
| 模型接入 | 多 provider 多模型池（GPT-6 Astra、Claude、Gemini、Qwen3-Coder、DeepSeek 等 13 家族） |
| 并行执行 | 多工作树（worktree）单元拆分 + 类型化结果回收 |
| 文档站 | GitHub Pages（rlaope.github.io/oh-my-hermes） |
| 多语言 README | 英/韩/日/中四语 |

---

## 项目亮点

### 路由打分 + 实测成本数据
README 公开了同一批编码任务上的对照数据：同样的 GPT-6 Astra，经路由打分后"18/30 同样解出"，成本从 $4.29 降到 $0.66、耗时从 23 分钟降到 5 分钟。把"该用强模型的用强模型、不该用的别浪费"量化到美元和分钟级别，这在同类 agent 增强项目中相当罕见。

### 类别链的用户主权
九条执行器类别链全部落在一个可编辑文件里，用户可以逐条覆盖；安装过程还会"面试"你的 provider 并按本机可用模型自动重排链条。provider 无法服务目标模型时直接拒绝派发而不是静默换一个——这种"宁可报错不可糊弄"的设计与项目整体的证据治理理念一致。

### 证据边界作为一等公民
从"Exit 0 但无证据只能算 reported done"到"验证回执三要素匹配才可复用"，OMH 把 agent 输出的可信度问题工程化了。对一个宣称"诚实记录实际发生了什么"的操作层来说，这些机制不是营销话术而是可检查的约束。

---

## 应用场景

### 个人开发者的降本提速
已经在用 Hermes Agent 的开发者，装上 OMH 后轻任务自动走便宜通道、重任务保持质量，长会话的编码/研究/交接工作获得结构化治理，模型账单显著下降。

### 团队的 Agent 工程化
需要把 AI 编码代理纳入工程流程的团队，可以用类别链统一各执行器的模型策略，用并行工作树安全拆分任务，用长期记忆沉淀项目知识，形成可审计的 agent 作业记录。

### 模型迁移与混布
同时持有多个 provider 配额的团队，通过逐家族校准块和自动链条重排，在模型更换/降级时保持行为一致性，避免"换个模型提示词全失效"的常见痛点。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| ⭐ 总 Stars | 1,801 |
| 🍴 总 Forks | 151 |
| 📈 今日新增 | +52 |
| 📅 创建时间 | 2026 年 6 月 3 日 |
| 📄 开源协议 | MIT |
| 🏷️ 主要标签 | agent, ai-agent, hermes-agent, claude-code, codex, skills |

---

## 总结

oh-my-hermes 是 Hermes Agent 生态中定位清晰的"强化操作层"：不夺舍、不隐藏执行器，用路由打分、逐家族校准、安全并行和证据门控四件套把一次自然语言请求变成可治理的工程流程，且每个关键主张都配了实测数据——三个月 1,800 星说明社区认可这种"诚实工程"的路线。

---

*数据来源：GitHub 仓库 (rlaope/oh-my-hermes)，2026 年 9 月 14 日访问*
