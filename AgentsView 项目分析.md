# AgentsView 项目分析

## 项目名称
**AgentsView** — 本地优先的 AI 编码代理会话智能分析与监控平台
- **GitHub**: [kenn-io/agentsview](https://github.com/kenn-io/agentsview)
- **许可证**: MIT
- **语言**: Go
- **主页**: [https://agentsview.io](https://agentsview.io)

---

## 项目概述

AgentsView 是一款本地优先（Local-first）的桌面和 Web 应用，专为浏览、搜索和分析 AI 编码代理的历史会话而设计。随着 AI 编码代理（如 Claude Code、Codex、Cursor 等）的爆发式增长，开发者日常工作中产生了大量代理会话数据，但这些数据散落在本地文件系统中，缺乏统一的查看和分析手段。AgentsView 正是为解决这一痛点而生——它自动读取代理在本地留下的会话文件，将其存入 SQLite 数据库并建立全文搜索索引，让开发者可以高效地回顾、搜索和理解自己的 AI 辅助编码历程。

在 Token 使用统计方面，AgentsView 提供了极快的查询能力。在包含 22,000 条会话记录的数据库上，`agentsview usage daily` 命令比同类工具 ccusage 快 80-220 倍，堪称 ccusage 的 100x 替代方案。这对于需要频繁检查 Token 消耗和成本的开发者来说，是一个实质性的效率提升。

该项目还具备 AI 驱动的洞察生成能力，可以调用 Claude、Codex、Copilot 或 Gemini 对会话进行智能总结和分析。此外，实时同步功能会持续监听会话目录的文件变化，确保数据库始终与最新的代理活动保持同步。对于团队协作场景，可选的 PostgreSQL 同步功能支持多机器环境下的数据共享。

---

## 核心功能

| 功能模块 | 描述 |
|---------|------|
| **多代理支持** | 支持 27+ AI 编码代理，包括 Claude Code、Codex、Copilot CLI、OpenCode、Gemini、Hermes 等 |
| **Token 与成本报告** | 极速统计每日/每周/每月 Token 使用量和费用，比 ccusage 快 80-220 倍 |
| **AI 洞察生成** | 调用 Claude/Codex/Copilot/Gemini 自动生成会话总结和分析报告 |
| **会话浏览器** | 完整的会话浏览界面，查看每一次代理对话的详细内容 |
| **全文搜索** | 基于 SQLite FTS 的全文搜索，快速定位历史会话中的特定内容 |
| **活动热力图** | 可视化展示代理使用频率和活跃时段分布 |
| **工具使用分析** | 分析代理在会话中调用了哪些工具及其使用频率 |
| **实时同步** | 监听会话目录变化，自动同步最新会话到数据库 |
| **聊天记录导入** | 支持 Claude.ai 和 ChatGPT 聊天记录的导入 |
| **PostgreSQL 同步** | 可选的团队/多机器数据同步方案 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 核心语言 | Go |
| 数据存储 | SQLite（本地） / PostgreSQL（团队同步） |
| 全文搜索 | SQLite FTS（Full-Text Search） |
| 桌面分发 | 跨平台原生构建 |
| 安装方式 | Homebrew（macOS）、二进制包（Windows/Linux） |
| 许可证 | MIT |

---

## 项目亮点

### 百倍性能的 Token 统计
在 22,000 条会话的真实测试场景中，`agentsview usage daily` 的执行速度比 ccusage 快 80-220 倍。这得益于 Go 语言的高性能和 SQLite 的优化查询，使开发者可以实时获得 Token 消耗反馈，而无需等待漫长的统计过程。

### 27+ 代理的统一视图
AI 编码代理生态高度碎片化，不同代理将会话数据存储在不同位置、使用不同格式。AgentsView 通过统一的适配层，支持包括 Claude Code、Codex、Copilot CLI、Cursor、Windsurf、Aider、Cline、Gemini CLI、Hermes 在内的 27+ 种代理，为开发者提供一站式的历史会话管理体验。

### AI 驱动的智能分析
不仅收集数据，还能"理解"数据。AgentsView 支持调用主流 LLM 对会话进行总结、提取关键信息、生成洞察报告。这意味着开发者不仅能看到"用了多少 Token"，还能获得"哪些会话最有价值"、"代理在哪些任务上表现不佳"等深层分析。

### 本地优先与隐私保护
所有会话数据存储在本地 SQLite 数据库中，无需将敏感的编码会话上传到第三方服务器。实时同步通过监听本地文件系统变化实现，完全在本地完成。对于需要团队共享的场景，PostgreSQL 同步是可选功能，用户拥有完全的数据控制权。

---

## 应用场景

### 个人开发者Token成本管理
使用 Claude Code 或 Codex 等付费代理的开发者可以实时追踪每日 Token 消耗和费用趋势，及时发现异常使用模式（如某个会话消耗了不成比例的 Token），从而优化 API 使用策略，控制成本支出。

### AI 辅助编码效果复盘
通过活动热力图和会话浏览功能，开发者可以回顾自己的 AI 辅助编码历程，分析代理在哪些任务上表现优异、哪些任务上需要人工介入更多。AI 洞察功能可自动生成这些分析报告。

### 团队AI代理使用标准化
团队管理员可通过 PostgreSQL 同步功能汇聚成员的代理使用数据，了解团队整体的 AI 编码工具使用情况、Token 消耗分布和工具偏好，为采购决策和最佳实践制定提供数据支撑。

### 多代理切换时的历史 continuity
经常在不同代理之间切换的开发者（如根据任务类型选择不同代理）可以利用 AgentsView 的统一搜索功能，跨代理查找历史解决方案和编码决策，避免在不同代理的会话历史中反复翻找。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| ⭐ 总 Stars | 1,451 |
| 🍴 总 Forks | 166 |
| 📅 创建时间 | 2026-02-19 |
| 📈 今日新增 Stars | 98 |
| 🖥️ 主要语言 | Go |
| 📜 许可证 | MIT |

---

## 总结

AgentsView 是 AI 编码代理时代的必备工具——当开发者的日常工作越来越依赖 Claude Code、Codex 等代理时，会话数据的浏览、搜索和分析需求变得迫切。AgentsView 以本地优先的方式，用 Go 的高性能和 SQLite 的轻量化，打造了一个支持 27+ 代理的统一会话分析平台，其百倍于竞品的统计速度和 AI 驱动的洞察生成能力，使其从单纯的"日志查看器"升级为"编码代理智能副驾驶"。对于重度使用 AI 编码代理的开发者而言，这是一个值得立即部署的生产力工具。

---

*数据来源：GitHub 仓库 (kenn-io/agentsview)，2026 年 6 月访问*
