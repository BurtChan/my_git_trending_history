# 项目名称

**Hivemind** — 为 AI 编码代理提供共享记忆和持续学习能力的云端智能层

- **GitHub 链接**: [https://github.com/activeloopai/hivemind](https://github.com/activeloopai/hivemind)
- **开源许可证**: Apache License 2.0
- **项目主页**: [https://www.deeplake.ai](https://www.deeplake.ai)

---

## 项目概述

Hivemind 是由 Activeloop（DeepLake 的创建者、Y Combinator 背投公司）开发的一款面向 AI 编码代理的共享记忆与持续学习平台。它的核心理念是"一个大脑驱动所有代理"——当团队中某个工程师的 AI 代理在周一解决了一个棘手的代码迁移问题后，周二团队中所有代理都能自动复用这个模式。Hivemind 不仅仅存储记忆，它还会从团队的交互轨迹中挖掘重复模式，将其提炼为可复用的技能（Skill），并自动传播到每一个连接的代理。

该项目的底层架构基于 Activeloop 自研的 DeepLake 向量数据库，结合了混合检索（词汇 + 语义）技术，在 LoCoMo 长上下文记忆基准测试中表现出色：相比基线方法，每 100 个问答的成本降低 25%，token 消耗减少 1.7 倍，对话轮次减少 31%。这背后的原理是，之前的工作成果在调用时已自动注入上下文，无需每个会话重新推导。

Hivemind 目前已支持 Claude Code、OpenClaw、Codex、Cursor、Hermes Agent 和 pi 等主流 AI 编码代理平台，通过 Marketplace 插件、Hook 或 Shell Hook 等方式无缝集成。安装过程极其简单——一条 `npx @deeplake/hivemind` 命令即可自动检测所有支持的代理并配置连接。项目自 2026 年 4 月创建以来，短短两个月已获得超过 600 颗 Star，并登上 GitHub Trending 榜单，反映了业界对 AI 代理记忆层这一新兴方向的强烈关注。

---

## 核心功能

| 功能 | 说明 |
|------|------|
| **自动捕获（Capture）** | 每次代理交互（prompt、工具调用、响应）自动作为结构化轨迹存储到 DeepLake |
| **技能挖掘（Skillify）** | 后台异步 Worker 分析近期会话，识别可复用模式，自动生成 SKILL.md 技能文件 |
| **知识传播（Propagate）** | 技能自动注入到所有 Hivemind 连接的代理上下文中，实现团队知识共享 |
| **自然语言搜索** | 支持用自然语言搜索团队历史轨迹，如"Emanuele 在做什么？"或"搜索我们解决的认证 Bug" |
| **会话摘要** | 每次会话结束后自动生成 AI 编写的 Wiki 摘要，存储为 768 维向量嵌入 |
| **代码库图谱** | 从交互轨迹中构建实时代码图谱（文件、符号、导入关系），搜索走图谱而非纯文本 |
| **团队规则（Rules）** | 跨代理共享的团队原则，自动注入到会话开始，违规行为作为关键错误提示用户 |
| **目标与 KPI 追踪** | 内置目标和 KPI 管理系统，支持状态流转（已开启→进行中→已关闭） |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 核心语言 | TypeScript |
| 向量数据库 | DeepLake（Activeloop 自研） |
| 检索方式 | 混合检索（词汇匹配 + 语义向量） |
| 嵌入维度 | 768 维 |
| 代理集成 | Claude Code Plugin、Codex Hooks、Cursor Hooks、Shell Hooks |
| 安装方式 | npx 通用安装器 |
| 许可证 | Apache License 2.0 |

---

## 项目亮点

### 团队知识的"复利效应"

Hivemind 最独特的价值在于它将 AI 代理从"独立工作者"转变为"团队学习者"。传统记忆方案是每个代理各自记住自己的经历，而 Hivemind 构建了一个组织级别的共享大脑。一个资深工程师代理习得的模式会自动流向初级工程师的代理，实现团队知识的复合增长。这种"解决一次，全组织共享"的模式在多人协作的软件开发场景中价值巨大。

### 自动化技能提取

Hivemind 的 Skillify 功能不是简单的对话记录存储——它会主动挖掘近期会话中的重复模式，并通过 AI 判断这些模式是否值得保留为技能。被选中的模式会被写入标准的 SKILL.md 文件，以结构化的方式提供给所有代理。这意味着代理的能力会随着使用时间自动增强，无需人工干预。

### 多平台统一支持

Hivemind 同时支持 Claude Code、Codex、Cursor、Hermes Agent、OpenClaw 和 pi 等六大主流 AI 编码代理平台，且通过一条命令即可完成全部安装和配置。这种跨平台的统一记忆层是业界首创，解决了不同代理之间记忆孤岛的痛点。

### 基于实证的性能提升

项目在 LoCoMo 长上下文记忆基准测试上展示了可量化的性能提升：25% 的成本降低、1.7 倍的 token 效率提升、31% 的对话轮次减少。这些数据来自标准化的学术基准测试，而非营销宣传，增强了项目的可信度。

---

## 应用场景

### 软件开发团队的知识共享

在多人使用 AI 编码代理的软件开发团队中，Hivemind 可以自动将每个代理解决问题的经验沉淀为团队共有知识。例如一个代理解决了复杂的数据库迁移问题后，其他代理在遇到类似任务时可以自动复用该经验，大幅提高团队整体效率。

### 企业级 AI 代理基础设施

对于正在大规模部署 AI 编码代理的企业，Hivemind 提供了一个中心化的记忆管理方案。通过团队规则（Rules）功能可以确保所有代理遵循一致的编码标准和安全原则，而目标与 KPI 追踪系统则帮助企业量化 AI 代理的产出价值。

### AI 代理性能优化

通过会话摘要和代码库图谱功能，Hivemind 为 AI 代理提供了远超单次对话上下文的历史信息。代理可以引用团队过去的决策、查找类似问题的解决方案、遵循已建立的最佳实践，从而减少重复劳动和错误率。

### 多代理协作场景

在需要多个不同 AI 代理协同工作的场景（如同时使用 Claude Code 做后端开发和 Cursor 做前端开发），Hivemind 的跨平台记忆共享确保了不同代理之间的知识连续性，避免了信息断层。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| GitHub Stars | 637 |
| Forks | 42 |
| 主要语言 | TypeScript |
| 今日新增 Stars | 47 ⭐ |
| 创建时间 | 2026 年 4 月 3 日 |
| 开源许可证 | Apache License 2.0 |
| 项目标签 | ai, ai-agents, ai-memory, claude, rag, embeddings, long-term-memory, postgres |

---

## 总结

Hivemind 是一个具有前瞻性的 AI 代理记忆与持续学习平台，它解决了一个日益突出的行业痛点：当团队中的每个人都使用 AI 编码代理时，如何让这些代理之间共享知识、避免重复踩坑。通过自动捕获交互轨迹、挖掘可复用模式、跨平台传播技能，Hivemind 实现了 AI 代理知识的"复利增长"。虽然项目尚处于早期阶段（0.x 版本），但其清晰的产品定位、实证的性能数据和多平台支持能力，使其成为 AI 编码代理生态中值得持续关注的基础设施项目。

---

*数据来源：GitHub 仓库 (activeloopai/hivemind)，2026 年 6 月访问*
