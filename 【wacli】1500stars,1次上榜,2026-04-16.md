# wacli 项目分析

## 项目名称
**wacli** — 一个基于命令行的 WhatsApp 工具，支持消息同步、搜索与发送。
- **GitHub**: [steipete/wacli](https://github.com/steipete/wacli)
- **许可证**: MIT License

---

## 项目概述

wacli 是一个用 Go 语言编写的 WhatsApp 命令行工具，基于 `whatsmeow` 库通过 WhatsApp Web 协议与 WhatsApp 进行交互。该项目的核心目标是实现本地消息历史的最佳同步、快速离线搜索、消息发送以及联系人与群组管理。作为一个第三方工具，wacli 不隶属于 WhatsApp 官方，但它提供了一种高效、隐私友好的方式让用户在终端中管理 WhatsApp 消息。

该项目由知名 iOS 开发者 Peter Steinberger（@steipete）创建，目前由 @dinakars777 维护。wacli 采用"尽力而为"（best-effort）的同步策略，能够持续捕获新消息，并支持按需回填较旧的历史记录。所有数据存储在本地（默认路径 `~/.wacli`），用户对自己的数据拥有完全控制权。

wacli 的设计理念强调简洁与实用性：默认输出人类可读格式，同时支持 `--json` 标志输出机器可读格式，方便与其他工具集成。项目已发布至 v0.6.0 版本，支持通过 Homebrew 快速安装，核心功能已趋于稳定。

---

## 核心功能
| 功能 | 描述 |
|------|------|
| 消息同步（sync） | 通过 WhatsApp Web 协议持续同步和捕获消息历史，支持非交互式后台运行 |
| 离线搜索（search） | 基于 SQLite FTS5 的全文搜索引擎，支持对已同步消息的快速离线检索 |
| 消息发送（send） | 支持向联系人或群组发送文本消息和文件，可自定义文件显示名称 |
| 历史回填（history） | 按需请求主设备（手机）同步更早的历史消息，以本地最早消息为锚点 |
| 媒体下载（media） | 同步消息后可下载消息中的图片、视频、文档等媒体文件到本地 |
| 联系人管理（contacts） | 列出和管理 WhatsApp 联系人和群组信息 |
| 群组管理（groups） | 列出群组、管理参与者，支持群组重命名等操作 |
| 二维码认证（auth） | 交互式登录，通过扫描 QR 码完成设备关联和初始数据同步 |
| JSON 输出 | 所有命令支持 `--json` 标志，输出结构化数据，便于脚本集成 |

---

## 技术栈
| 组件 | 技术 |
|------|------|
| 主要编程语言 | Go（100%） |
| WhatsApp 协议库 | whatsmeow（Go 语言的 WhatsApp Web 协议实现） |
| 本地存储/搜索 | SQLite（启用 FTS5 全文搜索扩展） |
| 构建标签 | `sqlite_fts5`（编译时启用 SQLite FTS5 支持） |
| 包管理/分发 | Homebrew Tap（`steipete/tap/wacli`） |
| CI/CD | GitHub Actions |
| 数据存储目录 | `~/.wacli`（可通过 `--store DIR` 自定义） |

---

## 项目亮点
### 1. 隐私优先的本地存储架构
所有消息数据存储在用户本地设备上，不经过任何第三方服务器。用户对自己的 WhatsApp 数据拥有完全控制权，非常适合注重隐私的用户。

### 2. 基于 SQLite FTS5 的高性能离线搜索
利用 SQLite 的 FTS5（全文搜索 5）扩展实现快速离线消息检索，无需网络连接即可搜索历史消息，搜索响应速度极快。

### 3. 灵活的双模式输出设计
默认输出人类可读的格式，同时通过 `--json` 标志支持结构化 JSON 输出，使其既适合日常手动使用，也能方便地与脚本和其他自动化工具集成。

### 4. 完善的历史回填机制
支持按需向主设备（手机）请求同步更早的历史消息，通过批量脚本可自动回填所有聊天记录的历史，最大程度恢复完整消息档案。

---

## 应用场景
### 个人消息归档与备份
用户可以将 WhatsApp 的所有聊天记录同步到本地进行归档备份，确保重要的消息和媒体文件不会丢失，并随时可以快速搜索查找。

### 终端重度用户的效率工具
对于习惯在终端中工作的开发者和技术人员，wacli 提供了在命令行中直接发送消息、搜索聊天记录的能力，无需离开终端环境。

### 自动化脚本集成
通过 `--json` 输出模式，wacli 可以轻松集成到 Shell 脚本、定时任务或其他自动化工作流中，实现消息监控、自动回复等高级场景。

### 数据分析与消息审计
企业和团队可以利用 wacli 同步群组消息，对沟通内容进行离线分析、统计和审计，满足合规或业务分析需求。

---

## Star 数据
| 指标 | 数据 |
|------|------|
| **总 Stars** | ~1,500 |
| **总 Forks** | ~191 |
| **今日新增** | 趋势上升中（GitHub Trending 项目） |
| **许可证** | MIT License |
| **主要语言** | Go |

---

## 总结

wacli 是一个实用且设计精良的 WhatsApp 命令行工具，由经验丰富的开发者 Peter Steinberger 发起。它基于 Go 语言和 whatsmeow 协议库构建，核心优势在于本地化的消息同步与存储、基于 SQLite FTS5 的高速离线搜索，以及灵活的双模式输出设计。项目采用 MIT 许可证开源，支持 Homebrew 一键安装，当前已发布至 v0.6.0 版本，功能日趋完善。无论是个人消息归档、终端效率提升，还是自动化脚本集成，wacli 都为 WhatsApp 用户提供了一个隐私友好、功能强大的命令行解决方案。
