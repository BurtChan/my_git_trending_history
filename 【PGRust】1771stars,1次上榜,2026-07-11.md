# PGRust 项目分析

## 项目名称
**PGRust** — 用 Rust 重写的 PostgreSQL 数据库，已通过 46000+ 项回归测试
- **GitHub**: [malisper/pgrust](https://github.com/malisper/pgrust)
- **许可证**: AGPL-3.0

---

## 项目概述
PGRust 是一个雄心勃勃的开源项目，旨在用 Rust 编程语言从头重写 PostgreSQL 数据库。该项目目前已实现 Postgres 18.3 兼容性，并在超过 46,000 项 Postgres 回归测试查询中完全匹配 Postgres 的预期输出。这意味着 PGRust 不仅在语法层面兼容 PostgreSQL，更在行为层面达到了高度一致。

项目的核心动机不是简单地将 C 代码翻译为 Rust，而是利用 Rust 的内存安全和类型系统特性，为 Postgres 的深度改造铺平道路。项目作者明确表示："目标是让 Postgres 从内部更容易修改——保持 Postgres 的行为模式，以真实 Postgres 测试作为验证标准，使用 Rust 加上 AI 辅助编程来探索更深层的数据库服务器变革。"这一理念体现了"先兼容后创新"的务实策略。

PGRust 具备磁盘兼容性，可以直接从现有的 Postgres 18.3 数据目录启动。项目还提供了 WebAssembly 在线演示（pgrust.com）和 Docker 镜像，方便开发者快速体验。不过项目目前明确标注为非生产就绪状态，性能优化和扩展兼容性仍在开发中。

---

## 核心功能

| 功能 | 描述 |
|------|------|
| Postgres 18.3 兼容 | 通过 46,000+ 项回归测试，行为与原生 Postgres 高度一致 |
| 磁盘兼容 | 可直接从现有 Postgres 18.3 数据目录启动，无需数据迁移 |
| WebAssembly 演示 | 提供浏览器端 WASM 演示（pgrust.com），无需安装即可体验 |
| Docker 支持 | 提供 Docker 镜像（malisper/pgrust:latest），快速部署测试 |
| 多线程路线图 | 规划了多线程 Postgres 内部架构改造 |
| 内置连接池 | 路线图中包含内置连接池功能 |
| AI 辅助开发 | 利用 AI 辅助编程加速 Rust 重写进程 |

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 核心语言 | Rust（99.8%） |
| 目标兼容 | PostgreSQL 18.3 |
| 构建工具 | Cargo |
| 演示技术 | WebAssembly |
| 容器化 | Docker |
| 依赖库 | OpenSSL、ICU、libpq |
| 许可证 | AGPL-3.0 |

---

## 项目亮点

### 46,000+ 回归测试的严格验证
PGRust 最令人印象深刻的成就不是代码量或功能数量，而是其通过 Postgres 完整回归测试套件的严格验证。Postgres 的回归测试包含超过 46,000 个查询，覆盖了从基本 SQL 操作到复杂查询优化的各种场景。PGRust 在这些测试中完全匹配 Postgres 的预期输出，这表明其兼容性不仅是语法层面的，更是语义和行为层面的深度兼容。这种严格的验证标准在数据库重写项目中极为罕见。

### AI 辅助编程的典范应用
PGRust 是 AI 辅助编程在大型系统软件领域的典型案例。项目作者公开分享了使用 AI 辅助将 C 代码重写为 Rust 的过程和经验（见 malisper.me 博客系列）。在一个拥有数百万行 C 代码的数据库系统中，AI 辅助编程显著加速了重写进程，同时保持了与原始系统的高度一致性。这种"AI + 人类协作"的开发模式为大型系统软件的现代化改造提供了新的思路。

### 前瞻性的架构改造路线图
PGRust 的路线图展示了超越简单重写的雄心。多线程 Postgres 内部架构、内置连接池、无 Vacuum 存储实验、快速分支和派生工作流——这些都是对 Postgres 核心架构的根本性改造。Postgres 作为一个有着近 30 年历史的 C 代码库，其内部架构的改造难度极高。PGRust 用 Rust 重写的目标正是为这些深度改造创造条件，利用 Rust 的类型安全和内存管理特性降低大规模重构的风险。

### 从兼容到创新的渐进策略
项目采用的策略值得称道：先追求与 Postgres 的完全行为兼容，以真实 Postgres 测试作为验证标准，然后再在此基础上探索架构创新。这种渐进式策略避免了"重写即重设计"的陷阱——先确保不破坏现有功能，再逐步引入新特性。磁盘兼容性意味着用户可以在 PGRust 和原生 Postgres 之间无缝切换，降低了迁移风险。

---

## 应用场景

### PostgreSQL 现代化替代探索
对于希望探索 PostgreSQL 现代化替代方案的团队，PGRust 提供了一个高兼容性的 Rust 实现参考。虽然目前尚未生产就绪，但其 46,000+ 回归测试的验证结果已经证明了 Rust 重写 Postgres 的可行性。数据库内核开发者和数据库即服务（DBaaS）提供商可以密切关注项目进展。

### AI 辅助系统编程研究
PGRust 作为 AI 辅助大型系统软件开发的实际案例，为研究者提供了宝贵的第一手资料。项目作者的博客系列详细记录了 AI 辅助重写过程中的经验教训，对于探索 AI 在系统编程领域的应用边界和最佳实践具有重要参考价值。

### 数据库架构创新实验平台
PGRust 的路线图中包含多项对 Postgres 核心架构的改造实验，如无 Vacuum 存储设计、多线程内部架构等。这些实验在原生 C 版本的 Postgres 中实施难度极大，但在 Rust 版本中可以更安全地进行。对于数据库研究者，PGRust 可能成为架构创新的实验平台。

### 教育与学习用途
对于想要深入理解 PostgreSQL 内部实现原理的开发者，PGRust 提供了一个 Rust 视角的参考实现。通过对比 PGRust 的 Rust 代码和原始 Postgres 的 C 代码，开发者可以同时学习数据库系统和 Rust 编程。WebAssembly 演示也降低了入门门槛。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| GitHub Stars | 1,771 |
| GitHub Forks | 45 |
| 今日新增 Stars | 789 |
| 主要语言 | Rust（99.8%） |
| 许可证 | AGPL-3.0 |
| 提交数 | 7,101 |

---

## 总结
PGRust 是一个用 Rust 重写 PostgreSQL 的雄心项目，已通过 46,000+ 项回归测试验证 Postgres 18.3 兼容性。其 AI 辅助编程的实践和前瞻性的架构改造路线图，为数据库系统的现代化改造提供了全新的思路和方向。

---

*数据来源：GitHub 仓库 (malisper/pgrust)，2026 年 7 月访问*
