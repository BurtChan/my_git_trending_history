# MemPalace 项目分析

> 数据更新时间：2026-06-06

## 项目名称

**MemPalace** — MemPalace/mempalace

## 项目概述

MemPalace 号称"最佳基准测试的开源 AI 记忆系统"，并且完全免费。该项目采用本地优先（local-first）架构，为 AI 代理提供持久化、可检索的记忆能力。其核心设计理念是"逐字存储"（verbatim storage），即原文内容不经过 LLM 改写直接存储，确保信息的完整性和准确性。MemPalace 在 LongMemEval 基准测试中取得了优异成绩，是当前开源 AI 记忆领域的标杆项目。

## 核心功能

- **本地优先记忆存储**：原文逐字存储，不依赖 LLM 改写，保证信息忠实度
- **可插拔后端**：默认使用 ChromaDB 作为向量数据库，支持替换为其他存储方案
- **LongMemEval 基准测试**：原始检索 R@5 达 96.6%，混合检索达 98.4%，结合重排序后 ≥99%
- **知识图谱**：基于时间序列的实体-关系图谱，追踪信息间的关联和演变
- **29 个 MCP 工具**：通过 Model Context Protocol 提供丰富的记忆操作接口
- **多代理支持**：每个 Agent 可拥有独立的记忆空间（Wings）
- **Claude Code 自动保存钩子**：`mempalace mine` 自动挖掘对话记忆，`mempalace sweep` 自动清理和整理

## 技术栈

| 类别 | 详情 |
|------|------|
| 主要语言 | Python |
| 许可证 | MIT |
| 创建时间 | 2026-04-05 |
| 默认嵌入模型 | all-MiniLM-L6-v2（无需 API Key） |
| 备选嵌入模型 | embeddinggemma-300m |
| 向量数据库 | ChromaDB（默认，可替换） |
| 协议支持 | MCP（Model Context Protocol） |
| 主题标签 | ai, chromadb, llm, mcp, memory, python |

## 项目亮点

1. **行业领先的检索性能**：LongMemEval 基准测试中纯向量检索 R@5 达 96.6%，这一成绩在开源方案中极为突出，无需调用 LLM 即可获得高质量检索结果
2. **零 API Key 启动**：默认使用 all-MiniLM-L6-v2 嵌入模型，开发者可以完全在本地运行，无需任何付费 API
3. **创新的记忆架构**：采用"宫殿式"层级组织 —— Wings（人物/项目）→ Rooms（主题）→ Drawers（原始内容），类比人类记忆宫殿的存储逻辑
4. **时间感知知识图谱**：不仅记录实体关系，还追踪关系的时间维度，使记忆具备时序推理能力
5. **原生 Claude Code 集成**：通过 Git 钩子自动捕获和保存编码对话中的知识，实现"编码即学习"
6. **海量社区认可**：53,890 Stars 和 7,079 Forks 表明该项目在 AI 开发者社区中获得了极高关注

## 应用场景

- **AI 代理长期记忆**：为 Claude Code、Codex 等 AI 编码工具提供持久化知识库
- **个人知识管理**：构建本地化的 AI 知识图谱，替代传统笔记工具
- **项目记忆系统**：团队成员可共享项目上下文，AI 代理自动积累项目知识
- **RAG 系统增强**：作为高精度的检索引擎，为检索增强生成系统提供底层支持
- **对话历史分析**：通过知识图谱分析对话中的实体关系和时间演变

## Star 数据

| 指标 | 数值 |
|------|------|
| 总 Stars | 53,890 ⭐ |
| 今日新增 | 227 ⭐ |
| Forks | 7,079 |
| 创建日期 | 2026-04-05 |

## 总结

MemPalace 是 2026 年开源 AI 记忆领域最具影响力的项目之一。自 2026 年 4 月创建以来，仅两个月便积累了超过 5.3 万 Stars，今日新增 227 个 Star，增长势头强劲。其核心价值在于将"记忆"这个 AI 代理的关键短板，通过本地优先、逐字存储、可插拔后端的架构设计，以开源免费的方式提供给了整个社区。96.6% 以上的检索准确率证明了其工程可行性，而 29 个 MCP 工具和 Claude Code 原生集成则保证了易用性。对于任何需要为 AI 代理添加持久记忆能力的开发者而言，MemPalace 都是当前最佳的开源选择。
