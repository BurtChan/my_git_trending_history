# MemPalace 项目分析

## 项目名称
**MemPalace** — 最佳基准测试的开源 AI 记忆系统
- **GitHub**: [MemPalace/mempalace](https://github.com/MemPalace/mempalace)
- **许可证**: MIT

---

## 项目概述

MemPalace 是一个开源的、本地优先（Local-first）的 AI 记忆系统，由 Milla Jovovich 和 Ben Sigman 共同创建并于 2026 年 4 月开源。项目的核心理念是「逐字存储」（Verbatim Storage）——不是对对话内容进行摘要或改写，而是完整保存原始文本，然后通过语义搜索进行精确检索。

MemPalace 的架构灵感来源于人类认知中的记忆宫殿（Memory Palace）概念，将记忆组织为分层结构：翼（Wings）对应人员和项目，房间（Rooms）对应主题，抽屉（Drawers）存储原始内容。这种分层设计使搜索可以被限定在特定范围内，而非在扁平语料库中进行全局搜索，从而大幅提升检索精度。

项目在 LongMemEval 基准测试上取得了令人瞩目的成绩：纯语义搜索（Raw）即达到 96.6% R@5 的检索召回率，无需任何 LLM 调用、无需云服务、无需 API Key。加入混合检索策略后（Hybrid v4），在 450 个 held-out 问题上达到 98.4% R@5。这些成绩在开源记忆系统领域处于领先水平。截至 2026 年 6 月，项目已获得超过 53,000 颗 Star。

---

## 核心功能

### 1. 逐字存储与语义检索
完整保存对话历史原文，使用 ChromaDB 作为默认检索后端进行语义搜索，确保检索结果精确还原原始语境。检索层完全可插拔，接口定义在 `mempalace/backends/base.py`。

### 2. 分层记忆宫殿架构
采用翼→房间→抽屉的三级分层结构组织记忆，支持按人员、项目、主题进行有范围的精准检索，避免全局搜索带来的噪音干扰。

### 3. 知识图谱
内置时序实体关系图谱，带有有效期窗口。支持添加、查询、失效和时线浏览，基于本地 SQLite 存储，为 Agent 提供结构化的世界知识。

### 4. MCP Server（29 个工具）
提供 29 个 MCP（Model Context Protocol）工具，覆盖宫殿读写、知识图谱操作、跨翼导航、抽屉管理和 Agent 日志等功能，可被任何兼容 MCP 的 Agent 直接调用。

### 5. Claude Code 自动保存钩子
提供两个自动保存钩子，定期保存对话记录并在上下文压缩前触发保存，确保 Claude Code 会话中的重要信息不会丢失。支持回填已有转录和逐消息召回。

### 6. 多 Agent 隔离
每个专业 Agent 拥有独立的翼和日志，通过 `mempalace_list_agents` 在运行时发现，避免系统提示词膨胀。

---

## 技术栈

| 组件 | 技术 |
|------|------|
| 语言 | Python 3.9+ |
| 向量数据库 | ChromaDB（默认，可插拔） |
| 知识图谱 | SQLite |
| 默认嵌入模型 | all-MiniLM-L6-v2 / embeddinggemma-300m |
| 协议 | MCP (Model Context Protocol) |
| 安装方式 | uv / pipx / pip |
| 许可证 | MIT |

---

## 项目亮点

### 业界领先的基准成绩
在 LongMemEval（500 题）上 96.6% R@5 Raw 召回率，LoCoMo（1986 题）上 88.9% R@10 混合召回率，MemBench（ACL 2025, 8500 项）上 80.3% R@5。这些成绩是可复现的，且核心检索路径完全不需要 LLM 或 API 调用。

### 本地优先、零 API Key
所有核心功能在本地运行，数据不离开用户机器。不需要任何 API Key 即可完成安装、索引和检索，这对于隐私敏感的使用场景尤为重要。

### 名人效应与社区热度
由知名演员 Milla Jovovich（生化危机、第五元素主演）联合创建，这一独特背景为项目带来了巨大的初始关注度和持续讨论热度，同时也引发了关于「名人开源项目」质量的社区讨论。

### 丰富的 MCP 工具生态
29 个 MCP 工具覆盖了记忆管理的方方面面，使其可以无缝集成到 Claude Code、Hermes Agent 等支持 MCP 协议的 AI 编码助手中，成为 Agent 长期记忆的即插即用解决方案。

---

## 应用场景

### AI 编码助手持久记忆
为 Claude Code、Cursor 等 AI 编码助手提供跨会话的持久记忆能力，保存项目上下文、用户偏好和开发决策，避免每次新会话都要重新解释项目背景。

### 个人知识管理
将日常对话、笔记和思考记录存储在本地记忆宫殿中，通过语义搜索快速检索过往信息，构建个人第二大脑。

### 多 Agent 系统的共享记忆
在多 Agent 协作系统中，MemPalace 可以为每个 Agent 提供隔离的记忆空间，同时支持跨 Agent 的知识图谱共享，实现结构化的信息传递。

### RAG 系统增强
作为 RAG（Retrieval-Augmented Generation）系统的记忆层，提供高质量的文本检索能力，其分层架构可以显著提升检索的精确度和相关性。

---

## Star 数据

| 指标 | 数值 |
|------|------|
| GitHub Stars | 53,647 |
| Forks | 7,066 |
| 今日新增 Stars | 228 |
| 主要语言 | Python |
| 许可证 | MIT |

---

## 总结

MemPalace 是目前开源 AI 记忆系统中基准成绩最佳的项目之一，以 96.6% R@5 Raw 召回率证明了其逐字存储+语义检索方案的有效性。本地优先的设计、零 API Key 的使用门槛和丰富的 MCP 工具生态使其成为 AI Agent 长期记忆需求的理想解决方案。无论是个人知识管理还是多 Agent 系统，MemPalace 都提供了成熟可靠的基础设施。

---

*数据来源：GitHub 仓库 (MemPalace/mempalace)，2026 年 6 月访问*
